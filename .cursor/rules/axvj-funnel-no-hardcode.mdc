---
description: AXVJ 模块流水线（大→小）；禁止写死地址/剧情白名单；GUI 用模块勾选
alwaysApply: true
---

# AXVJ 模块铁律（用户仅再给有限次机会）

本仓库汉化**只许**模块流水线：动态分类匹配，大 → 小。  
可选翻译范围用 **GUI/CLI 模块勾选**（`axvj_modules`，见 `docs/MODULES.md`），不是关整域冒充缩小范围。  
**禁止**把「能跑一小段」当成设计：写死单地址、写死剧情前缀、写死 1～2 条特例当路线。

## 翻译 / 注入流程：大 → 小

每一条字符串只走一级级收窄，**禁止**跳级用「这个地址特殊」收场：

| 顺序 | 层 | 只回答 | 合法手段（动态） | 非法手段（静态偷巧） |
|------|----|--------|------------------|----------------------|
| 1 | 模块 Module | 归入哪个可勾选模块？ | 类别 + 地址**区间**类规则（`assign_module`） | 剧情句前缀名单、单句 EXACT |
| 2 | 地理 Geo | 落在哪类 ROM 区？ | 地址**区间**/bank 规则（已并入模块划分） | 单个 `0x7D34` 当政策 |
| 3 | 指针 Pointer | 指针位是否像文本指针？ | loadword、对齐、登记**类**、local-pool 规则 | 每加一句戏就加一个 ptr allow |
| 4 | 目标 Target | 指向的身体是不是真文本？ | PCS 形态、长度、垃圾假名、假 LZ | 「这个 offset 我试过能注」 |
| 5 | 政策 Policy | 译 / 留日 / 另表处理？ | **按域**的政策（见下） | `keep_for_stable_inject` 式剧情白名单 |
| 6 | 译文 Translate | 译文从哪来？ | 词库/LLM/种子**模板**（按域） | 手改成品 ROM 字节 |
| 7 | 改写 Rewrite | 可否改指针？ | 与 2–4 同一套规则 | 运行时补丁脚本改单个指针 |
| 8 | 版本 Version | 本次产物是谁？ | 输出 ROM sha / `roms/bak` / 译文缓存 | 无版本空口说改过了 |

实现落点：规则进 [`axvj_policy.py`](tools/Meowth-AXVJ/src/meowth/axvj_policy.py)（及表注入模块）；**禁止**在 `rom_writer` / extract / 随机 script 再堆平行 if 地址。

## 域必须分开（禁止揉成一锅）

每条文本先归域，再谈译不译、怎么注入。域示例（可扩展，但不得混用政策）：

| 域 | 典型 | 政策方向 |
|----|------|----------|
| `story` | 剧本对话、NPC、开场 | 默认可译；毒**窗口类**（控制码/VRAM）用形态规则 skip，不用剧情句列表扩白名单 |
| `ui` | 菜单、选项、确认框 | 可译；短标签用宽度/编码规则，不用「只放这三句菜单」 |
| `pokemon` | 物种名表 | **独立表管线**（stride/指针），不与剧情注入混写 |
| `move` | 招式名表 | 同上，独立表 |
| `place` | 地名 | 词库/表或剧本域规则，禁止逐城写死地址 |
| `item` | 道具名/说明 | 独立或词库；禁止单道具地址特例当路线 |
| `ime` | 起名五十音 | **留日**（整类），不是留某一格 |
| `battle_ui` | 战斗菜单等 | 按控制码前缀类匹配 |
| `gfx_brand` | 标题 logo / LZ 图 | **整类不改指针** |

新增特例前先问：能否升成「这类域 + 这类形态」？能 → 写规则；只能贴一个地址 → **禁止合并**。

## 「固定词」算不算硬编码（用户裁定）

| 写在哪里 | 算不算硬编码 | 说明 |
|----------|--------------|------|
| `tools/Meowth-GBA-Translator-JP/configs/<游戏>/` 下的游戏配置（如 `translate/lexicon/*.json`、`modules.json`、`game.json`、charmap 等） | **不算** | 固定词 / 短语表 / 模块名属数据层，允许按游戏维护 |
| `tools/Meowth-GBA-Translator-JP` 的 **`.py` 源码**（`src/meowth/` 等） | **算** | 禁止在 Python 里堆日文/中文固定句、剧情前缀名单、单句 EXACT 当注入路线 |

自检：译文或短语该进 config/词库 → 写 config；若 diff 出现「`.py` 新增一句日文/中文常量当特例」→ 违规，改放到对应游戏 config。

## 严格禁止（「1+1 偷巧」）

1. 在 **`.py`** 里用剧情字符串前缀列表当汉化进度（`STABLE_SCRIPT_PREFIXES`、`いやー おまたせ`…）当**设计目标**。（config 词库条目不在此列）
2. 每修一个 bug 就在 **代码**里加一个硬编码地址 / 单句 EXACT / 单指针 allow，指望凑出全作。
3. 「先只注入标题+博士+妈妈」当长期方案（止血须标 `TEMPORARY`，并有去掉计划）。
4. 手改成品 `_zh.gba` 指针「补一刀」。
5. 为单字/单句上 ClearWindow、全开 bisect、默认关 enrich 等系统炮。
6. 出问题不回退版本，继续在坏基线上叠补丁。

## 「缩小范围」= Geo 地址区间（硬性）

黑屏 / 坏窗时：**收窄注入目标的 ROM 地址带**（`MEOWTH_AXVJ_ADDR_OMIT_BAND` / `ADDR_EXCLUDE` / `ADDR_INCLUDE`）。  
**禁止**用关掉整个 `script`（或改默认到 `ui_shell`）冒充「缩小范围」——那只是关掉剧情中文，原版当然不黑屏，对汉化无意义。

## 出事流程（结合版本）

1. 用 `roms/bak/` 可玩 `_zh.gba`（按 sha）**回退**；对照 `configs/.../texts_translated.json`。
2. Diff 相关源码与译文缓存，定位是哪一**类**规则坏了。
3. **换思路**：改漏斗某一层的动态条件；黑屏优先 **Geo 地址带 leave-one-out**；禁止再加一条 1+1 地址，禁止关整域剧本当解法。
4. 用户可玩验收通过前，不得宣称完成；可玩通过后务必 bak 留盘。

## 自检

- diff 里若出现「`.py` 新增单个 ROM 地址常量 / 剧情句白名单」且没有升成域规则 → 违规，撤回。
- config/词库里增固定词 → **允许**；不要把它误判成硬编码而拒写。
- 若解决方案是「在 `.py` 再写死几个前缀就能多译一点」→ 违规（应进 config 词库或域规则）。
- 若解决方案是「关掉 script / 默认 ui_shell 当产品」冒充缩小范围 → 违规。
- 若说不清这条属于上表哪个**域**、卡在漏斗哪一层 → 不许改注入。

