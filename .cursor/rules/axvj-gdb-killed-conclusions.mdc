---
description: gdb/实机已证伪结论黑名单 — 禁止当前提重推
alwaysApply: true
---

# AXVJ：gdb/实机已证伪结论 — 禁止当前提重推

与 MEMORY「勿重推」同步。代理若再把下列条目当设计地基 → 违规，先停。

## 禁止再当前提

1. **CY2 花 = 查看器伪影** / **根因 = tile 象限序**  
   已推翻。CY2（GBA4bpp）== 游戏；真因常为 **nibble 左=低半字节** 等数据/绘制问题。

2. **静态「独占带」/抬水位** 当地基  
   含已证伪的 cb0 `[0x2D2,0x400)`、以及 **假划界**（占段 + 全局 `SetBaseTileNum` 区间顶开）。  
   详见 `axvj-no-fake-window-port.mdc` / `docs/禁止_假划界抬水位冒充美版窗.md`。

3. **日版 `textMode=2` = 美版窗画布**  
   日版 tm2 = **血条 `win[0x20]` 缓冲**；美版设置页 tm2 = **IWTD `2+w×h` 画布**。禁止改日版模板 textMode→2「对齐美版」。

4. **tm1 用 `TILE_BASE+TILE_OFFSET` 冒充官方线性**  
   官方 tm1 不碰 OFFSET；Init 每块清 OFFSET=0 → 多字段叠印（设置页 / Bug H）。禁止再以此当 P0「窗内线性」。

5. **`us_mask` / mask 进仓库 = 引擎移植完成**  
   仅换画笔。有 `v8_alloc_*` / 位图避让 = **未完成**移植。

6. **「tm1 上 v8 物理必需」挡画布路线**  
   与美版 gdb（设置 tm2 画布零分配器）及用户拍板「废分配器 + 窗画布落址」冲突。禁止用此句挡 T-a。

## 必须

- 新结论标【推论/未验证】直至 gdb 或用户实机截图验收。
- 与上表冲突的旧 MEMORY/文档段落 → 以本规则 + 当日 gdb 为准，勿回魂。
