---
description: 禁止测避让带与抬水位，只认美版 VRAM 分配器（所有权+游标+生命周期）
alwaysApply: true
---

# AXVJ：禁止避让带 / 抬水位，唯一路线是复刻美版 VRAM 分配器

2026-09-09 用户裁定：两条死路必须同时封死，防止在它们之间来回打转。

## 两条禁令（违反即停）

1. **禁止无脑抬水位的假做法**：占段 + 全局顶基址 + `SetBase` 永久改写 + 改 textMode 冒充。
   → 详见 `axvj-no-fake-window-port.mdc`。
2. **禁止测避让带**：扫描/采样 VRAM 找「空闲带」再把文本 tile 塞进去。凡属此类一律禁止，
   包括但不限于：v8 分配器、W0 预算表、cb-survey、`602 连续空闲`、`cb3 尾巴占用` 等一切变体。

## 为什么两条是同一条死路

- **避让带** = 被动「测哪里空就往哪塞」，结果随场景动态变化，测出来的是快照不是契约。
- **抬水位** = 主动「固定顶基址占一段」，全局改写，冒充美版。
- 两者都是**没有所有权 / 生命周期的裸 VRAM 抢占**，区别只在抢的时机。
- 实测反例（用户提供）：**「领航员」**存放大量宝可梦/训练家介绍文本，空闲带不够放。

## 唯一正确路线：复刻美版 VRAM 分配器

美版 `text_window.c` 已确认的契约（研读中）：
- `TextWindow_SetBaseTileNum(base)` 设全局游标 `sTextWindowBaseTileNum`，返回 `base+9`（框 9 tile）。
- `TextWindow_SetDlgFrameBaseTileNum(base)` 返回 `base+14`（对话框 14 tile）。
- 窗口**开→分配一段→返回下一段起点**；**关→游标回退/复用**。

要移植的是**这套分配器本身**（含所有权与生命周期），不是「替日版共享图集找空位」。

## 自检

- diff 若出现「扫描/采样 VRAM 空闲区间 → 按区间塞 tile」→ 违规（避让带）。
- diff 若出现「固定 span 顶基址 / 按区间永久 SetBase」→ 违规（抬水位）。
- 正确形态：出现美版式 `sTextWindowBaseTileNum` 游标 + `base+span` 返回 + 关闭复用。
