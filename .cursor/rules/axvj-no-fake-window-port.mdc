---
description: 禁止用抬水位/占段顶基址冒充美版 Window 划界移植
alwaysApply: true
---

# AXVJ：禁止假划界（抬水位冒充美版窗）

2026-09-08 用户裁定：多次口头「真划界」后仍交占段+顶基址。本规则与
`docs/禁止_假划界抬水位冒充美版窗.md` 同步；违反即停，禁止再包装成移植。
事故过程（pending / 写 e6f0 / 假换 Continue）：`docs/复盘_20260908_假接Continue与抬水位混验收.md`。

## 什么算冒充（禁止当路线）

1. **占一段 VRAM**（固定 `[lo,hi)` / `span≤512` / 强行 602 全幅擦）再让文字进这段。
2. **全局抬/顶基址**：`SetBaseTileNum` / 分配游标「凡落入我段则改为 start+span」且账本跨场景常驻。
3. **只有 GCTN / blank×2 / us_mask** 却宣称「引擎/Window 移植完成」。
4. **绕开改开窗入口**：不改 Multistep/IWTD 契约，专改 PrintNextChar 绘制侧「修好花屏」。
5. 把日版模板 `textMode` 改成 2「对齐美版」。

## 真划界必须同时具备

1. **入口是窗（或等价窗对象）**：预算挂在当次 `TextPrinter`/`gMenuWindow`，不是「全局独占带」。
2. **开窗一次**得到 `2+w×h`（w×h 有证伪来源：美版孪生 / FRAME / W0 表）。
3. **当次**把「下一段起点」交给框装载（等同美版 IWTD 返回值）；**用完清除**，不污染能力页等其它场景。
4. 落址 = 窗内位置公式；**禁止** v8/freelist 当 tm1 画布主路径。

## 自检

- diff 若出现「`SetBaseTileNum` 按区间永久改写」且无「一次性 pending + 清除」→ 违规。
- 若修法是 cap span / 抬 lo / 全局顶基址 → 违规，先停问用户。
