---
description: 禁止为修显示擅自改 lexicon/短语；显示问题用绘制 op/补丁
alwaysApply: true
---

# 短语 / lexicon：禁止擅自改译

## 必须

- **无用户当轮明确指令**时，禁止修改 `configs/**/translate/lexicon/**`、短语表或其它译文文件来「凑显示 / 躲裁切 / 躲超框」。
- 窗宽不够、字被裁、叠字等 **绘制/布局问题**：用 texts.json modules 的 `write.type=op`（新 op + `PrintNextChar`/`draw_glyph` 分支）或字体/窗几何修复。
- 用户明确说「改译文 / 改短语 / 改成××」时，才允许动 lexicon。

## 禁止

1. 把「宝可梦」改成「精灵」等缩写冒充修裁切（除非用户点名改文案）。
2. 为过闸门、过冒烟、省事而改短语内容。
3. 在回复里先改了译文再问用户要不要——先停，请示或走绘制方案。

## 自检

- 本轮 diff 若含 `lexicon/*.json` 或短语映射，而用户原话没有「改译/改短语」→ 违规，撤回。
- 显示类需求默认答案应是：**新 write.op / 绘制路径**，不是改词。
