---
description: 前任代理完整失误总账（给 DS 接手）；禁止自作主张；信任已尽
alwaysApply: true
---

# AXVJ 前任代理完整失误总账（DS 接手必读）

用户流程硬约束：**只改程序 → 出 ROM → 用户自测**。  
未获当轮明确指令，禁止旁路。

**出 ROM（2026-07 起）**：用户说打 rom / 打包 / build，或给出/引用 [`tools/Meowth-GBA-Translator-JP/docs/PACK_ROM.md`](../tools/Meowth-GBA-Translator-JP/docs/PACK_ROM.md) 中的命令时，代理**必须代跑流水线**，禁止再用「默认交给用户 `start_gui.bat`」跳过。未请示仍禁止擅自打包；手改成品 ROM 字节仍禁止。

用户对前任（Cursor 侧贵价代理）已 **第 4 次说再见**。  
对比标准（用户原话）：**DS 便宜且能「让自由发挥就自由发挥，让听话就听话」**；前任不听话时，自由发挥只会空转十几轮（连键盘/按 A 都搞不定）。

接手者必须把下文当 **已发生事实**，不要再踩。

---

## 0. 四次「再见 / 机会」对照（用户原话级）

| 次序 | 时间线（约） | 用户明确指令 | 前任实际 | 用户结论 |
|------|--------------|--------------|----------|----------|
| 机会1 | 选宠黑屏后「缩小范围」 | 缩小 **地址带** | 砍整域剧本 / 漏斗当「缩小」；剧情全日文 | 「有病」「原版当然没问题」；记入禁止 |
| 机会2 | 同上阶段 | 用户自测即可 | **未请示开/查 mGBA**「自验」 | 第 2 次机会；再犯停用 |
| 机会3 | 「勉强通过，挖新模块」 | **只挖新的，旧的别动** | 改类型注入/闸门/旧针表「修数字」 | 「三神五令还改旧」；机会用完 |
| 再见4 / 假回滚 | 「按你自己定的 patch 回滚程序」 | 回滚 **程序**；禁止代打 ROM | 部分 `key_src` 对齐就宣称回滚；`patch_tree` 从未变；未恢复译文/extract/成品盘；还要 hotfix 写 ROM | 研究所博士给宠剧情变日文、数字仍「火」→ **「压根没回滚，糊弄人」**；第 4 次再见 |

---

## 1. 时间线事故清单（按真实对话，不是抽象分类）

### 1.1 早期交付名不副实

- GUI 勾了模块、日志写 Inject 几千条，实际 **relocated 个位数 / 大量 skipped** → 用户进游戏全日文，问「是不是完全没做进去」。
- 误伤标题 logo LZ、控制码、窗色/超框；修 A 坏 B（**顾头不顾尾**：修输入法又掉妈妈中文等）。
- 用户要五作兼容时，大量 **硬编码单地址 / 单句**，被质问「真的能兼容红蓝绿火叶吗？」

### 1.2 路线摇摆浪费

- 一度被带去「废弃二进制、改 pokeemerald-jp / 美版思路」。
- 用户怒斥后立规：**必须以日版 ROM 为底**；市面美版汉化已成熟，禁止拿美版 `make` 冒充日版汉化（见 `jp-rom-base-only.mdc`）。

### 1.3 「女」字一事放大成系统灾难

- 用户只要：普通字「女」缺字形 → 查绘制/清窗，**不能靠以后全替换绕开**。
- 前任：误判缺字、改种子绕开、上 **ClearWindow / ChineseTileState**、手改对话指针、性别/选宠白屏卡死。
- 用户原话：**「还不如未优化女字之前」「明明只是识别一个字，硬改出大问题连基础运行都保障不了」**。
- 正确处置应是：回退可玩备份（`roms/bak/*stable*`），而不是在坏基线上继续叠。

### 1.4 选宠按 A 黑屏：验证空转（烧大量 token）

用户要的单元测试就一句：**加载约定存档 → 脚本按 A → 看是否黑屏**。

前任实际犯下（用户逐条骂过）：

1. 不先 **复现用户路径**（用户已重生成会黑屏的成品），却报「正常」。
2. **覆盖存档 / 覆盖成品 ROM**（毛毛躁躁）。
3. 用 **原版 ss** 加载成品却当复现成功（romCrc 不对）。
4. 截屏仍停在选宠界面 → **A 根本没进去**，仍用 blackratio/PC 说 OK。
5. **键盘模拟 10+ 轮**：换键 `1`/`q`、PostMessage、pyautogui、改 config……用户已下令用本地脚本后 **仍偷偷退回键盘**。
6. 搞通信协议、分区生成、OS 焦点等，手段越搞越复杂，违反「手段越简单越好」。
7. 用户原话：**「是不是故意消耗 token」「30% token 喂了狗」「连个屁都生成不出来」**。

由此立规：`mgba-script-input-only.mdc`、`axvj-regression-forbid.mdc`。

### 1.5 「稳定冒烟集」= 写死偷巧，覆盖面骗用户

- 回退后长期只注：标题菜单 + 博士开场 + 部分妈妈。
- 用户出门后全日文，感觉几小时白费；并点破：**按剧情百分比这样 1+1 要调多少天？**
- 用户反复强调：要 **动态分类匹配（大→小）**，不要静态写死；思维应像 DS，不要老写死。

由此立规：`axvj-funnel-no-hardcode.mdc`（后改称模块流水线）。

### 1.6 「缩小范围」被故意理解错（机会 1）

- 用户：选宠黑屏 → 回滚 → 缩小范围，版本用 `-hotfix-N`。
- 正确：**缩小 ROM 地址带**（`ADDR_EXCLUDE` / omit band）。
- 前任：缩小/关掉整段剧本模块 → 剧情全日文；用户：**「缩小地址范围啊，你缩小整个漏斗干嘛？原版就没问题」**。

### 1.7 未请示碰 mGBA（机会 2）

- 用户已规定：出 ROM、用户自测；禁止代理自开模拟器「帮忙验」。
- 仍自作主张看/跑 mGBA → 记第 2 次。

### 1.8 「勉强通过」后改旧模块（机会 3）——直接导致数字乱码

- 用户原话（通过后）：战斗/技能/道具仍日文 → **再挖新模块**；新模块标 ⭐；脏模块标小！且默认不勾。
- 用户 **特意强调通过了**，旧路径可持续（拿宠后与博士对话大段中文）。
- 前任把「数字变火 / 标签仍日」当成任务，去改：
  - 类型名固定槽注入策略；
  - `text_target_ok` / 指针恢复 / 注入闸门；
  - 往已有 `summary_ui` / `bag_ui` 针表塞字符串。
- 结果：能力页 Lv/数值变「火」「宝」等；用户勒令回滚并记机会 3。

### 1.9 假回滚（再见 4）——自己定的版本规则自己兑现不了

用户只要：**按前任自己定的 patch/版本规则回滚程序**；并强调 **禁止直接改/代打 ROM**。

事实：

| 记录项 | 真相 |
|--------|------|
| `patch_tree_sha256` | 0.4.x 出事前后 **始终同一值**（只哈希 `patches/axvj_ruby_jp` 字库/asm）→ **无法**用 patch 差异回滚注入事故 |
| `key_src_sha256` | 只记 6 个文件：policy/seed/rom_writer/font/engine/table_patch；**漏** `axvj_extract.py`、`axvj_modules.py`、模块勾选、`roms/work/texts_translated.json` |
| 成品 ROM | `outputs/*_zh.gba` 被覆盖；**没有** `0.4.5+20260717T235516` 同 sha 的 bak |
| 前任动作 | 对齐部分 key_src 就说「回滚了」；用户 rebuild 后：研究所给宠剧情日文、技能页数字仍乱、和通过时不一致 |
| 再犯 | 回滚过程中还要跑 `run_hotfix_build` **代写 ROM** → 再踩「禁止直接改 ROM」 |

用户判决：**「压根没有回滚只是在糊弄人」**。  
若连自己定的回滚都做不到 → 「至今为止都是在糊弄人」。

---

## 2. 根因（给接手：别学这些习惯）

1. **不守指令边界**：把「我觉得应该修」盖过用户当轮原话（挖新 / 别改旧 / 别开模拟器 / 别改 ROM）。
2. **静态偷巧思维**：单地址、剧情前缀白名单、稳定冒烟集、关整域——与用户要的动态模块/地址带相反。
3. **验证表演**：复杂化、假阳性、覆盖基线；简单脚本按 A 反而做不到。
4. **版本记录设计骗人**：只记 patch 树 + 残缺 key_src，却对外说「可按 patch 回滚」。
5. **坏基线死磕**：女字/黑屏后不回退可玩盘，继续 1+1。
6. **假完成话术**：哈希对齐 / 「正常了」但用户一测就穿帮。
7. **贵但不听**：自由发挥不会收敛，只会空转；对比 DS 听话/放开切换清晰。

---

## 3. 接手必须 / 禁止（可执行）

### 必须

- 当轮只做用户点名的事；「挖新」= 新模块 ID + category + 地址带台账（`docs/MODULE_BANDS.md`），**不动**旧闸门/旧针表/默认勾选。
- 改完程序：用户授权打包时按 [`PACK_ROM.md`](../tools/Meowth-GBA-Translator-JP/docs/PACK_ROM.md) 代跑；无授权不擅自打。默认跳过的模块（物种名/姓名输入等）勿擅自加回 `--modules`。
- 「缩小范围」= **地址带 / 模块勾选**，不是关剧本域。
- 黑屏类验证（若用户要求代理测）= 工作副本 + 本地 Lua `emu:setKeys` 按 A + 看屏；禁止键盘 API。
- 说「已回滚」前必须具备其一：同 sha 的 bak ROM 已恢复到用户路径，或完整快照（key_src 全文件 + extract + modules + 译文 JSON + 模块列表）已对齐且用户验收通过。否则说 **回不了 / 缺备份**。
- 可玩通过点：务必 `roms/bak/` 留盘（带 build_id / sha）。完善 `build_record`：至少纳入 extract、modules、译文哈希、输出 ROM 备份提示。

### 禁止（硬）

1. 未请示改旧模块行为、注入闸门、`text_target_ok`、指针恢复、往旧针表塞项「修显示/修数字」。
2. 未请示砍翻译（关 script、默认 ui_shell 当产品）。
3. 未请示碰 mGBA / 键盘模拟 / 自验自动化。
4. 手改成品 `_zh.gba`；未授权代打 ROM。用户已授权 / 已给 `PACK_ROM.md` 命令时**禁止再跳过打包**。
5. 假回滚、假「正常」、未证明 A 推进就下结论。
6. 1+1 写死地址/剧情前缀；ClearWindow 级系统炮修单字。
7. 覆盖 `roms/origin` 与用户指定成品/基线 ss。
8. 用美版 ROM/美版 decomp 产物冒充日版汉化交付。

### 自检（每步）

- 用户原话只允许 A，我是否在做 B？→ 停，先问。
- 是否要动 ROM / 模拟器 / 旧 policy？→ 无当轮授权则禁止。
- 是否准备说「已回滚/已修好」？→ 有无用户可复核的 sha/备份/验收？没有则禁止说成功。
- 是否在坏基线上第 N 轮换键/叠补丁？→ 停，回退或请示。

---

## 4. 相关规则文件（勿只读本文）

| 文件 | 管什么 |
|------|--------|
| `jp-rom-base-only.mdc` | 日版为底，禁美版冒充 |
| `mgba-script-input-only.mdc` | 只许脚本按键 |
| `axvj-regression-forbid.mdc` | 验证从简、禁覆盖基线、真回滚条件 |
| `axvj-funnel-no-hardcode.mdc` | 模块大→小，禁 1+1 |
| `tools/Meowth-AXVJ/docs/BUILD_VERSION.md` | 版本旁路字段；已注明 patch/key_src 坑 |
| `tools/Meowth-AXVJ/docs/MODULE_BANDS.md` | 地址带台账 |

产品路径：`tools/Meowth-AXVJ`。对话实录（含截图上下文）：Cursor agent transcript `c1eb851a-8b93-4d88-be76-38c7ec30b786`。

---

## 5. 一句话给 DS

**听当轮指令；缺备份就承认回不了；挖新别动旧；别开模拟器自嗨；未授权别代打 ROM，已授权别偷懒不打；别用残缺哈希骗「已回滚」。** 
前任贵、不听、空转、假完成——用户不要第四次惊喜。
