---
description: AXVJ 回归禁止项；手段从简；禁止覆盖基线；与漏斗铁律配合
alwaysApply: true
---

# AXVJ / 模拟器回归：禁止项与手段从简

与 [`axvj-funnel-no-hardcode.mdc`](axvj-funnel-no-hardcode.mdc) 同时生效：漏斗管怎么汉化，本文件管怎么验证。

## 原则：手段越简单越好

- 「按 A 黑屏」→ 单元测试：约定存档 + **脚本按 A** + 黑屏判定。
- 禁止为验证发明通信协议、PC/CRC 玄学、冷启动跑开场、键盘换键大战（见 `mgba-script-input-only.mdc`）。
- 先复现用户路径；复现失败禁止报「正常」。

## 禁止（验证与交付）

1. 键盘/鼠标模拟按键；用户要脚本后不得退回。
2. 未证明画面因 A 推进就下结论。
3. 原版 ss 加载成品却当复现成功（romCrc 必须匹配被测 ROM，除非用户声明）。
4. 覆盖 `roms/origin` 基线 ss / 用户指定成品 ROM；测试只用 `roms/work` 副本。
5. 手改成品 ROM；只改程序，经流水线重建（用户授权时按 `docs/PACK_ROM.md` 代跑，勿手改字节）。
6. 用写死剧情白名单「偷巧」冒充进度（见漏斗铁律）。

## 必须（版本）

- 交付物只生成 `{gameId}_translated.gba`（不写 `.gba.build.json` / `build_history.jsonl`）。
- 出问题先回退可玩 ROM 备份（`roms/bak/`），再换漏斗层思路；禁止坏基线上无限 1+1。
- 小范围验证版本号用后缀：`0.3.7-hotfix-1`（按 Domain 缩窄）；已验证可玩的命名漏斗为 **`ui_shell`（界面壳层）**，见 `axvj_funnels.py`。
- **真回滚**还须：可玩 `_zh.gba` 进 `roms/bak/`（按 sha）、译文缓存 `configs/.../texts_translated.json`、key_src；仅口头对齐≠已回滚。失误总账见 [`axvj-no-unsolicited-deviations.mdc`](axvj-no-unsolicited-deviations.mdc)。
