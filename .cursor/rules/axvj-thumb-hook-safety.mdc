---
description: Thumb ROM 桩跳板铁律：续跑地址必须奇数；序言只 push{lr} 的钩子禁止残留 r4-r7
alwaysApply: true
---

# AXVJ：Thumb hook 跳板安全（2026-09-09 领航员重启事故）

事故：给 `TextLoadWindowTemplate@0x08002950` 加的桩，进领航员界面**立即重启**。
两处 bug 叠加，两条都是写桩的常识，再犯不可接受。

## 铁律一：续跑地址必须奇数（bit0 = 1）

`bx rN` / `pop {pc}` 落到**偶数**地址 = 切到 ARM 模式 → Thumb 代码瞬间失控 → 重启。

```asm
ldr r5, =0x0800295A   @ ✗ 偶数 → 切 ARM 模式 → 必崩
bx  r5
ldr r5, =0x0800295B   @ ✓ 奇数（目标 0x0800295A | 1）
```

对照表（本仓库现状）：`0x08003301` / `0x080036DD` / `0x08002C75` 正确；
曾写错的 `0x08003BDC`、`0x08003BB4`（`Text_ClearWindow_Hook`）已改为
`0x08003BDD`、`0x08003BB5`。

## 铁律二：跳板不得残留寄存器污染

判据是**被钩函数的序言**：

- 序言 `push {r4,r5,r6,lr}`（如 `InitTextPrinter`）→ 原函数自己保存 r4-r6，
  跳板用 r4/r5 做跳板是安全的（原函数会覆盖并恢复）。
- 序言**只有** `push {lr}`（如 `TextLoadWindowTemplate@0x08002950`）——
  r4~r11 无人保存，跳板残留的 r4/r5 会**泄漏给调用者**
  （调用者返回后继续用 r4/r5 → 读到垃圾 → 崩）。

零污染标准写法（用栈做跳板，`pop {...,pc}` 兼做恢复 + 跳转）：

```asm
MyHook:
    push    {r4, r5, r6, lr}       @ [sp+12] 是 lr 槽，下面改写成跳转目标
    ldr     r4, =0x0800295B        @ 续跑地址，必须奇数
    str     r4, [sp, #12]
    push    {r0, r1, r2, r3}       @ 保参（C 会砸 r0-r3）
    bl      my_c_hook
    pop     {r0, r1, r2, r3}
    @ 此处重放被桩/pool 覆盖掉的原序言指令
    pop     {r4, r5, r6, pc}       @ 恢复 r4-r6 + 跳转，栈完全平衡
```

## 附加检查清单

- 桩覆盖掉的原指令（含被 `.pool` 4 字节冲掉的下一条）必须**逐条重放**，
  且重放顺序/语义要与反汇编一致（`objdump -D -b binary -m arm -M force-thumb`）。
- `pop {r0..r3}` 不影响标志位，但若后面还要 `bne` 复用条件，确保中间没有
  `adds/subs/cmp` 之类覆盖标志。
- 改完必须 `objdump` 复核机器码 + 实机进对应界面验证，不能只凭编译通过就交付。
