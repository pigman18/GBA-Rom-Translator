---
description: 验证分层——gdb 是探针不是流程环节；判据在画面就不开 gdb
alwaysApply: true
---

# AXVJ 验证分层：什么时候该开 gdb

## 病根

把「①静态 ②gdb ③实机截图 ④入记忆」当成**每轮必跑的清单** ⇒
每改一版都要开一次 mGBA+stub 采集，**成本全由用户付**，收益常常是零。

真相：**gdb 只有一个用途 —— 画面上看不出区别时，回答「到底有没有生效」。**
它是探针，不是流程环节。与 [`axvj-regression-forbid.mdc`](axvj-regression-forbid.mdc)
「手段越简单越好」同源。

## 分层判据（从上往下，能判就停）

| 层 | 手段 | 能判什么 | 用户成本 | 何时做 |
|---|---|---|---|---|
| **L1 静态** | 字节 / 反汇编 / 编译 / `hook/out/game.map` | ①代码进没进 ROM ②桩形态对不对 ③符号落在哪 | ~0 | **必做** |
| **L2 画面** | 用户开 ROM 看一眼 | **改动到底对不对** | 一次操作 | **默认验收** |
| **L3 gdb** | mGBA + stub + preset 采集 | 画面对不上时，**为什么** | 开模拟器 + 复现路径 | **仅当 L2 判决不了** |

本项目**所有施工步的目标都是「画面变好」**（框不撞字 / 容得下更多汉字 / 图形落点正确 / 久跑不崩）
⇒ 判据天然在 L2。

## 调研期 vs 施工期（阶段变了，手段必须跟着变）

- **调研期**（如摸官方链：哪个函数分配、入参是什么、调用序如何）问的是
  「机制到底是什么」= **数值问题** ⇒ L1+L3 不可替代。
- **施工期**（动手改号/改分配）问的是「改对了吗」= **画面问题** ⇒ L2 就是判据，
  **L3 是多余的**。

别把调研期的习惯带进施工期。

## 硬约束

1. **开口要求采集前，必须能一句话说出「这次采是为了回答哪一个具体问题」。**
   说不出来 ⇒ **不许要**。
2. 采集日志是**给代理看的**（用户原话：「我完全看不懂你采集的是啥」）
   ⇒ **成本用户付、收益代理收** ⇒ 只在真的判决不了时才开口。
3. **静态能证的，不许用 gdb 证。** 例：桩在不在 ROM = 查 `file 0x62080` 的字节
   （S3 应为 `10 b5 …`），不用跑模拟器。
4. **禁止「改一版、采一次」× N 轮。** 若某阶段确实要采，**一次采集覆盖该阶段全部判据**
   （同一个 preset 里放齐），而不是每步都采。
5. 用户习惯**直接关 mGBA** ⇒ 采集必须「边跑边打增量」，不许依赖 Ctrl-C。
6. 未请示不碰 mGBA（见 [`axvj-no-unsolicited-deviations.mdc`](axvj-no-unsolicited-deviations.mdc) §3）。

## 自检（每次出 ROM 前）

- 这次改动的**画面预期**能写成一句话吗？
  - **能** ⇒ 请用户看一眼即可，**不开 gdb**。
  - **不能** ⇒ 说明自己也不知道改对了长什么样 ⇒ **先回去改设计**，不是去开 gdb。
- 画面与预期不符，**且想不出下一步该改哪** ⇒ 这时才开 gdb，带着那个具体问题去。
- 已经连续两轮「出 ROM → 采集 → 没结论」⇒ 停，回到「画面预期」这一问。

## 🔴 打包前声明（必填六项，缺一项即不许打包）

> 为什么加这一节：本文件原有自检条「连续两轮没结论 ⇒ 停」**从未触发过**，
> 因为它要求代理判断「我没结论」——**而代理每一轮都认为自己有结论**（只是结论是错的）。
> ⇒ **凡需要代理自我判断的闸门，一律拦不住代理。必须是"填格子"。**

出 ROM 前，在回复里**逐项写出**（不是心里想）：

| # | 必填 | 判据 |
|---|---|---|
| 1 | 用户当轮原话（**逐字抄**） | —— |
| 2 | 本轮做的东西，一句话 | —— |
| 3 | **2 与 1 的宾语是否一致？** | **否 ⇒ 立即停，先问**（2026-09-11：用户说「测 hook」，代理去「测空间」⇒ 宾语不符仍在推进） |
| 4 | 本轮改动的**画面预期**，一句话 | **写不出 ⇒ 停，回去改设计**（不得改为"去开 gdb"） |
| 5 | **代码区（file < 0x800000）diff 段清单**：段数 / 字节数 / 各段地址 | 注入区体积**不算数** |
| 6 | 有没有出现「抬水位」形态（起点常量 / `max(常量,…)` / 「避开·钉在…之后」措辞）？ | **有 ⇒ 停**（见 [`axvj-no-avoidance-band.mdc`](axvj-no-avoidance-band.mdc) 字符串级判定） |

**第 1、3 两项是机械对照，不需要判断——这是专门为"代理自认为在推进"设计的。**
另一条同样机械的计数：**同一个问题，改代码次数上限 1；要改第 2 次，必须先写出
「第 1 次改动的画面预期 vs 实际」，写不出即停。** 不许有第 3 次。

## 相关

- `axvj-regression-forbid.mdc` —— 手段从简、禁键盘模拟、禁覆盖基线
- `axvj-no-unsolicited-deviations.mdc` —— §3 禁止 3「未请示碰 mGBA」
- `scripts/GDB_*.md` —— 各 preset 手册；**是工具书，不是流程单**
