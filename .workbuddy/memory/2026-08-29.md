# 2026-08-29

## 切换分支 → feature/text-v3-hy4

### 1. 移植 32MB 对齐修复
换分支后 Python 侧修复丢失。确认 `feature/text-v3` 保留完整修复，且两分支的
`src/meowth/rom_writer.py` / `src/meowth/core/engine.py` 差异**仅**为该修复（+60/-2，
无 hy4 独有改动），故安全移植：

```bash
git checkout feature/text-v3 -- src/meowth/rom_writer.py src/meowth/core/engine.py
```

已验证 `align_rom`(rom_writer.py:300) 与 `_align_rom_file`(engine.py:1597) 就位、
py_compile 通过。**尚未重打 ROM**（工作区有用户未提交的 hook/out 产物，跑流水线会
重编 game.bin 覆盖它们，先不动）。

修复要点回顾（详见 2026-08-28.md）：
- 对齐必须放在 **tiles 之后**（`_tiles_safe_alloc_base` 按当前文件大小定位，
  在 build_rom 提前 pad 会把 tiles 顶出 32MB）
- `align_rom()` 原地 extend 并返回同一对象 → 必须先存 `before = len(data)`

### 2. 文本引擎架构（hook/src/text/，2026-08-28 定型的 textMode 分派制）

**主控 `PrintNextChar_hook.c`**（全局唯一 ROM hook：P01@0x032F8 → entry.s EngineEntry）
- 特例：tm1+font4+tilemap0 → `PrintNextChar_Origin` 整窗交官方（血条/队伍窗）
- 控制码 0xFA/0xFB(翻页箭头,含 `arrow_inplace12` 相位对齐)、0xFC(→HandleExtCtrlCode
  FC 子类型 1..16)、0xFD/0xFE/0xFF；0xEF 菜单光标
- `TranslateHandleChar` 翻译链路（F9 协议/slot）→ `DrawGlyph`
- `DrawGlyph`：SYM 标点带自绘；≥0xF7 直接消费（缺字=排查信号）；
  否则 `sPcsPrintFuncs[textMode & 7]` 两级分发

**落址桥接 `FontFunc_hook.c`** — `Chs_FontFunc_hook(win, glyph, is_chs)` 按
`win[0x0A]` 分派四套**原生**协议（反汇编实证，见 docs/调研_20260828_原生tm0协议与替换BUG根）：

| tm | 入口 | 落址公式 | 推进 |
|---|---|---|---|
| tm0 | 0x08003569 | `win[0x16]+win[0x18]` | `win[0x18]+=2; win[0x1B]+=1` |
| tm1 | 0x0800360D | ROM 预渲染查表，零 VRAM 写；中文借用线性+地板 `TM1_LINEAR_FLOOR=0x100` | `win[0x1B]+=1` |
| tm2 | 0x0800338C | `win[0x20]` 缓冲直绘 | `win[0x20]+=0x40` |
| tm3 | 0x08003494 | grid：`base+col+2+row*30` | `win[0x1B]+=1` |

- 12px 字模两趟 spill：pass1 宽 8（溢出 spill 到 N+1）、pass2 宽 4；
  相邻字共享 tile，必须经 `DrawGlyphTile_refpr` 移位，不能直接 copy_tile32
- 行相位存 8 槽 LRU `@0x0203FF84`（每槽 8B：key/px/btx/last_adv/gen/char_base），
  `phase_bind` 命中时**必须做连续性校验** `curTX == btx + ((px+last-1)>>3)`，
  否则残留 px 会累加 → 文字一路向右铺满整屏

**两个致命约束（写 C 时务必遵守）**
1. `game.bin` 是 freestanding 平坦镜像（link/game.ld 无 RAM 段）→ **全局变量落 ROM
   写入被硬件丢弃**。字形缓冲一律用栈上局部（`struct TextGlyph g`），
   跨调用状态只能放已验证安全区 `0x0203FF80-0x0203FFCF`
2. tm3 专用原语 `sub_80033B4` 会写 `dst+960`（upper/lower 在 VRAM 相隔 30 tile），
   对 64B 缓冲会写穿 → 非中文一律用 `sub_8003630`（写 dst 与 dst+32）

### 3. 发现文档与代码不一致（待办，未擅自改）
`configs/POKEMON_RUBY_AXVJ00/hook/README.md` §10 描述的仍是 2026-08-25 的
**页游标制**（`GlyphPageCur` @0x0203FFD2 / `GlyphScratchRange` / `GlyphScratchAlloc`
/ 槽位分区表 §10.4）。这些符号在 src/ 与 include/ 中**已全部不存在**，
现行为 2026-08-28 的 textMode 分派制（场景识别 `scene_gctn_mode2` 等已删）。
README §1 目录树也仍写作 `src/text.c`，实际是 `src/text/PrintNextChar_hook.c` 等。
§10.4 的 charBase 槽位占用实测表（F1-F10）仍有参考价值，但结论部分已失效。

### 4. 其他
- `docs/` 新增三份（hy4 分支）：`调研_20260828_原生tm0协议与替换BUG根.md`、
  `调研_20260828_日版AXVJ文本引擎分发表与美版桥接评估.md`、`PLAN_ROM_DISPATCH_BRIDGE.md`
- `on_error()` 是 `pass`（callbacks.py:40），异常被静默吞、退出码仍为 0 —— 排查勿信 EXITCODE

---

## 设置菜单乱码 BUG 修复（用户实测待验收）

### 现象
设置菜单所有选项行显示同样的碎片字形（"快 匦底 弋"），移动光标原生重绘后
依旧错误（"不看底 弋"）。gdb 日志：`src/util/work/POKEMON_RUBY_AXVJ00/gdb_patcher_log.log`

### 根因（新旧引擎对比 + gdb 实证）
设置菜单选项 = `DrawOptionMenuChoice_hook` 构造 dst（栈上，日志中 `文本=0x03007DD0`
即 IWRAM 栈）→ `MenuPrint_Text` → 每行一次独立 `InitTextPrinter` 会话，
win=0x0202E658、**textMode=1**、fontNum=3、TILE_BASE=1（882 条全部如此）。

- **gdb 实证**：每会话 `TILE_OFF(win[0x18])` 被原生重置回固定值——
  分布仅 5 个取值：0x102×538、0x106×324、0x108×11、0x10C×5、0x0×4。
- **新版**（8-28 分派制）tm1 中文 = 借用线性，游标就是 `win[0x18]`，且只有
  「off<0x100 才提升」的地板逻辑（0x102>0x100 根本不触发）→ **每行都从同一
  tile 起写**：行间互相覆盖 + 踩预渲染字库区 → 全屏同样的碎片。
- **旧版**（bak/text_original）`screen_menu_mode2` 命中走 **mode2 grid 位置式
  寻址**，tile 位置只由 cursor 字段决定，与 win[0x18] 无关 → 会话重置无害。
  8-28 重构删掉 grid 路线时，tm1 中文改线性借用，此坑随之而来。

### 修复（`FontFunc_hook.c`，最小侵入，保留分派制）
tm1 原生实证零读写 win[0x18]（调研_20260828 §6.1），借用安全 → 加**持久分配游标**：
- `TM1_ALLOC_CUR 0x0203FFC6`(u16) / `TM1_ALLOC_PAGE 0x0203FFC8`(u16)
  （PHASE 槽占 0x84..0xC3、gen@0xC4，此两字节空余）
- `chs_blit` tm1 分支：页指纹（template 指针^TILE_BASE）变化 → 游标重置
  0x100；否则恢复持久游标进 win[0x18]（替换原先只提地板的逻辑）
- blit 完成后把推进过的 win[0x18] 存回持久游标 → 同窗跨会话顺序推进，
  行间不再共用 tile
- 已知残余风险：同 charBase 的**同时可见**多窗（设置菜单顶部回显窗+主窗）
  页指纹不同、各自从 0x100 起写，理论上互踩；比修复前全屏乱码轻，待实测

### 修复 v3（确定性分区，替换 v2 行槽——v2 残余问题被第二轮 gdb 否决）
**v2 残余问题实证**（第二轮日志，同路径 01:09 采集）：
- **标签和值是同 curY 的不同会话**（值列 curX=15/18/19/20/22/23 vs 标签 curX=4，
  同 curY=5/7/9/11/13/15）→ 并发 key ≈ 8 标签+8 值+顶窗 ≈ 17 个，8 槽必轮转
  → 行内容串换（用户"移动光标会使得文本替换"）；顶窗槽被抢 → 顶窗残留
- **会话重置值有公式**：off = 0x100 + (curTX-1)*2，全部样本吻合
  （curTX=2/4/5/7 → 0x102/0x106/0x108/0x10C）。它可能恰好落在子区区间内，
  区间判定漏判 → 行首残留。另见 off=0x0 的特殊会话（IWRAM 短文本）。
- v2 行槽方案的"每行 12 tile"假设也错：标签+值分属两会话，各需独立区。

**v3 = 无状态确定性分区**（`tm1_row_base`，零分配器零槽零 RAM）：
- base = 0x100 + (curY>>1)*0x10 + (curX>=8)*0x8 —— 行 9 档 × 标签/值两子区，
  每子区 8 tile=4 字（"文字速度"恰好 4 字；值最长"立体声"3 字）
- 总占 0x90 ⊂ [0x100,0x1C8)；同会话重绘恒映射同子区 → 幂等、不漂移、不外溢
- 会话开始判定：off == 0x100+(curTX-1)*2（显式公式比对，防区间内漏判）
  或 off 出子区区间 → 恢复 base
- 已知边界：值文本变短时尾部字形残留（表项未清）；>4 字标签会溢出子区
  （当前菜单最长恰好 4 字）；curY>17 的 tm1 场景 row 全挤到 8

### 待办
- [ ] 用户实测设置菜单 v3（初进、停留、上下移动、滚动、值切换）
- [ ] README §10 页游标制描述已过时（现行为 textMode 分派制），待用户确认后更新
- [ ] 若 v3 残留仍显眼：考虑会话开始时清除子区 tile/表项（需算 tilemap 地址）

### 构建备注
- 直接对 hook/ 跑 armips 不行：`./charmap.txt`、`graphic/fonts.s` 是流水线
  staging 产物，必须走 `meowth full`
- Git Bash 里 `cmd //c build.bat` 不会真正执行（只出 cmd 欢迎头），须用
  PowerShell `& .\build.bat`；armips 路径是 `../../../tools/armips.exe`（3 层）

---

### 5. 重构合理性评估（应用户要求，反汇编复核）
产出：`docs/评估_20260829_text重构合理性反汇编复核.md`（完整版）

**反汇编新发现（修正既有认知）**：
- **N1** FontSub[0] 实际 `lower = upper + 0x100`（`movs r3,#0x80; lsls #9`），
  调研_20260828 §6.1 写 +1 是笔误；tm0 表项同样 +0x100（N2）。
  tm0/tm1 字库 = 上/下半行分离布局；仅 tm3 连续 grid。
- **N3** UpdateTilemap@0x080036DC：表项值 = tile|pal<<12；lower 表项写在
  tilemap 下一行同列（地址+0x40）。新版中文写 lower=upper+1 与原生不一致
  但画/指自洽 → 不发作。
- **N4（最大暴露面）** tm1 字库引用只落奇数槽：upper=1+glyph*2 ∈[1,0xFF]，
  lower=+0x100 ∈[0x101,0x1FF]（glyph 0..127）。中文区 [0x100,0x190) 与 lower
  奇数槽 [0x101,0x18F]（glyph 0..71 下半行）**结构性重叠**。现在不发作靠
  「字符集稀疏 + 场景重入重绘自愈」双重侥幸。

**决策判定**：分派制✅ / 协议复现✅（文档两处笔误）/ **tm1 借线性❌（三轮 BUG
总根源：游标载体每会话被原生清）** / 删页游标✅、**删 scene_remap_tile⚠️（未
验证就删）** / 非中文走相位✅。
bak 两个哲学判断是对的且被丢掉：位置式优于分配器（v3 确定性分区实为手工回归）、
实测章位应沉淀为声明式表。

**遗留风险排序**：R1 字库下半区重叠 > R2 UI 章 > R3 值变短残留 > R4 charBase
未实证（待 gdb 采 BGCNT）> R5 重置值公式单窗。
**中期正解**：为 tm1 中文重建位置式落址（tile=格子），一次消解 R1/R2/R4。

### 修复 v4（偶数槽，R1 修复——评估报告预言被实测证实）
**实测照三张**：① 设置菜单"默认字库的数字没了"（数字/字母残缺）
② 图鉴 No004+ 未知名"？？？"变"喹喹喹喹" ③ 图鉴说明文字错乱。
用户明确"没有替换 BUG 了" → v3 幂等分区已生效（串换/外溢解决）✓
①② 正是评估报告 **R1 发作**：中文区 [0x100,0x190) 覆盖字库 lower 奇数槽
[0x101,0x18F]（glyph 0..71 下半行）→ 数字/问号下半行被中文 tile 顶掉。

v4 改两处（`FontFunc_hook.c`）：
1. `tm1_row_base` 基址改占**偶数槽**：`base = 2 + (curY>>1)*0x10 + (curX>=8)*0x8`
   （r=0..8 → base ∈ [2,0x8A] 全偶数；子区 8 tile；基址 <0x100 而原生会话
   重置值恒 ≥0x102 → 区间判定天然区分，无需公式比对）
2. `chs_tile_num` tm1 分支：模拟原生上下分离 `lower = upper + 0x100`（原 +1）
   → 上半 [2,0x90]、下半 [0x102,0x190] 全偶数，与字库奇数槽 **零碰撞**

**问题③（图鉴说明）未修**：tm0 场景，窗参（TILE_BASE/charBase/textMode）
未采，不盲改；需用户进图鉴说明界面再采一轮 gdb 日志后定位。

### 方法论沉淀（这次真正管用的）
- **反汇编给布局不变量**（字库只引奇数槽、lower=upper+0x100、表项差 0x100
  与地址差 0x40 是两回事），**gdb 给会话行为**（重置值、并发 key 数）。
  前者定空间边界、后者定时间行为。v1 铺满、v2 串换都是只有后者没有前者。
- tm1 中文落址四次演进：线性借游标 → 持久游标(外溢) → 行槽(串换)
  → 确定性分区(踩字库) → **偶数槽+上下分离(v4)**。
  结论：**任何"分配游标"都扛不住无限重绘，最终答案必然是位置式的**；
  且分配区必须用反汇编证明"未被原生引用"，不能凭"实测没出问题"。

### 修复 v4.1（v4 全屏崩溃后的修正——根本错在 chs_tile_num 仍用 win[0x16]）
**v4 灾难**：用户实测 v4 → 设置页面"全乱码 111、连图形都被影响"。
v4 让 chs_blit 把 win[0x18] 设为偶数 base=2，但 chs_tile_num tm1 分支公式是
`base(=win[0x16]=1) + off(2) + ... = 3`（奇数 = 字模 1 上半），所有中文表项
**全部**指 tile 3 / 0x103 = 字模 1 上下半槽 = "1"。整屏显示同一字模的"1"，
连边框/箭头被 OAM 覆盖是因为 tile 3 区域的字模数据被中文覆盖。
**根因**：`TM1_LINEAR_FLOOR`/`tm1_row_base` 返回的"中文虚拟基址"根本没传进
chs_tile_num——它仍按 `win[0x16] + win[0x18]` 算 tile。v3 也是同病（v3 凑巧
写到了字库区另一处，数字/问号被覆盖 = R1），v4 凑巧写到字模 1。

**v4.1 修正**（`FontFunc_hook.c`）：
1. `chs_tile_num` tm1 分支**去掉 + base**：return 直接用 `off + col*2 + row*0x100`，
   即 win[0x18] 直接当 tile 索引（off 已被 chs_blit 设为 chs 偶数基址）。
2. chs_blit 仍把 win[0x18] 恢复为 `tm1_row_base(win)`（偶数基址），不变。
3. 移除 chs_blit 中冗余的 `off == reset` 公式比对（区间判定天然区分：
   子区 base <0x100，重置值恒 ≥0x102）。

### 用户拍板：回退 A（v3 稳定态），R1 转专项
2026-08-29 用户决策：**回退 v3**（唯一"串换已修、基本可读"的版本），
接受 R1 代价（中文区压在字库 lower 奇数槽 → 数字/问号被踩），
tm1 中文落址作为**专项**重做，先 gdb 验证再设计。已回退：
- `chs_tile_num` tm1 恢复 `base + off + col*2 + row`（去掉 v4.1 的 off 直用）
- `tm1_row_base` 恢复 `TM1_LINEAR_FLOOR(0x100) + row*0x10 + col*8`
- `chs_blit` 恢复含 `off == reset` 公式比对的判定
- 校验：game.bin = **14424 字节，与 v3 那次完全一致** ✓；32MB 对齐 ✓
- 注释写了回退原因与专项步骤，避免后人再踩 v4/v4.1

**gdb_patcher 验证配套**（用户同意配合采集）：
- 关键监听点**都已内置**，只是 yaml 里 `default: false`，用 `--functions` 启用
- **ChsFontFunc 地址已同步修正**：yaml 旧值 0x088019b0（8槽LRU 版）已过期，
  当前 out/game.map 实际 = **0x08801978**。**game.bin 每次重编都必须重新核对。**
- 采集命令：
  `python src/util/gdb_patcher.py log --functions InitTextPrinter,UpdateTilemap,ChsFontFunc,InitWindowTileData,InitWindowTileDataRet`
  预期回答：UpdateTilemap(r1=upper,r2=lower) → 原生 vs 中文各写了哪些 tile（谁持有）；
  InitWindowTileData+Ret → 字库预渲染真实范围与"下一空闲 offset"（真实自由区）；
  ChsFontFunc → 中文每字符 glyph/相位/tile

### 第二轮 gdb（02:04/02:05 两轮，813KB）——推翻我的 N1，给出数据驱动解
采集命令：`python src/util/gdb_patcher.py log --functions InitTextPrinter,UpdateTilemap,ChsFontFunc,InitWindowTileData,InitWindowTileDataRet`
命中：UpdateTilemap 519、ChsFontFunc 309、IWTD 256+256、ITP 264。

**F1（我的 N1 是错的，必须记住）**：实测 `lower = upper + 1`（连续成对），
不是 +0x100。我反汇编时把 `0x80<<9` 算成 0x100（实为 **0x10000**），且没注意
r2 已 `lsls #0x10`，加 0x10000 = 高16位+1。**原调研文档的 +1 是对的**。
UpdateTilemap 差值分布：469 条差=1（tm1）、50 条差=30（tm3），无一 +0x100。
→ v4/v4.1「偶数槽安全」= 把中文搬进**字模下半行 tile**，这是 v4 全屏"1"的深层原因。

**F2（v3 的 R1 实锤）**：v3 中文 tile 序列**连续**（0x23,0x24,0x25...0x131），
而字库是每字模 2 连续 tile（upper=1+2g 奇 / lower=2+2g 偶）→ 中文连续区间
覆盖了途经字模的完整上下半 → 数字/字母/符号字模被顶掉 =「数字没了」「？？？→喹」。

**F3（tm 分布）**：ChsFontFunc tm=1 占 280/309（91%），tm=3 仅 29 条
（集中在轮1）。**方向没打空**，tm1 仍是正确的主战场。（曾一度误判 tm=3 为主）

**F4（数据驱动的安全区，最有价值）**：字库形式上占满 [1,0x201)，但设置菜单
**实际只引用 22 个字模**（upper 去重集合）：
`1 33 49 111 119 323 325 327 329 331 333 335 337 339 345 349 369 397 409 439 447 451`
每字模 2 连续 tile → 实占 44 tile。**最大安全连续块 = [121, 322]（202 tile）**，
另有 [3,32]/[51,110]/[453,512]。→ 中文落 [121,322] 内即零冲突，无需猜奇偶。
（该集合仅对设置菜单场景有效，其他 tm1 场景需各自采集。）

**F5（子区容量）**：12px 字相位交错下 off 推进为 +2/+4 交替 → **4 字需 12 tile**
（v3/v4 子区 0x8 不够，是 v4.1 越界覆盖的直接原因）。
`col_delta*2` 的 spill 设计**是对的**（spill tile 就是该字 pass2 的 tile，像素互补）。

### v5 待选方案（未实施，等用户拍板取舍）
中文 base=0x80（在 [121,322] 安全块内），行 stride=0x18：
- A：标签 12px(0xC) + 值 12px(0xC)，8 行 → [0x80,0x140)=[128,320] ⊂ 安全块 ✓
  但 9 档（curY 到 17 → row 8）会撞 323-340 ✗ → 需 clamp 到 8 行
- B：标签 12px(0xC) + **值改 8px 小字**(0x6)，行 stride 0x12 → 9 档 [128,290] ✓
  代价：设置菜单的值显示变小字（视觉取舍）
- C：值列沿用片假名/符号原字模（不汉化值）→ 需求最小，但值仍是日文

### 方案 D 已实施（02:38 出 ROM，待用户实测）
用户问"你确定D吗？移动光标会追加颜色编码"→ 验证后确定 D：
- **palette 不影响 tile**：FC 05 调色板只改表项高 4 位（tile|pal<<12），不占配额 ✓
- **按模板分离场景**后（_tmp 脚本，已删）实测设置菜单上界：
  模板 `0x081BB874`(tm=1) 标签恒 4 字（F9 80 短语：对话速度/战斗动画/对战规则/
  声音/按键模式/窗口/关闭/改变设置），值 IWRAM **5-8 字节、87% 为 7B = 1 字** → ≤2 字。
  19B 长文本属图鉴说明（另一场景），不算设置菜单的值。
  `0x081BB46C`(tm=3) = 开始菜单/图鉴，走 grid 路径，不受此分配区影响。
- 容量：标签 12 + 值 8 = 行 stride 20，9 档 = 180 → **[121,300]，余量 22 tile** ✓
  与原生 22 字模（44 tile）**零冲突**（脚本校验）

**D 的三处改动**（`FontFunc_hook.c`）：
1. `TM1_CHS_BASE=0x79(121)` / `TM1_ROW_STRIDE=0x14(20)` /
   `TM1_LABEL_SPAN=0xC(12)` / `TM1_VALUE_OFF=0xC` / `TM1_VALUE_SPAN=0x8`
2. `tm1_row_base` 只算行基址（`121 + row*20`，row=curY>>1 clamp 8），
   **col 偏移改由行内 off 表达**（标签 0 / 值 12）
3. `chs_tile_num` tm1 分支 = `tm1_row_base(win) + off + col*2 + row_delta`
   （**不再加 win[0x16]**）；`chs_blit` 把 win[0x18] 恢复为行内偏移 start
4. ⚠ 必须在 chs_tile_num 前加**前向声明** `static uint16_t tm1_row_base(...)`，
   否则 implicit declaration 编译错误（build.bat 静默失败、game.bin 不更新）

**旧版 12px 是否靠 scene（用户提问的答案）**：
12px **字体**本身不依赖 scene（`glyph->height = (fontId==4)?8:12`）；
但旧版能让设置菜单正确**落址**依赖 scene——`scene_should_use_linear` 命中
`screen_menu_mode2` → `scene_gctn_mode2` = **grid 位置式**，位置式无"分配区"
概念故不受容量限制。这才是旧版能吃下 12px 的原因。

**教训（重要）**：之前四版（v1/v2/v3/v4）都隐含一个**跨抽象层的假设错**：
`TM1_LINEAR_FLOOR`/`tm1_row_base` 算的"中文基址"想表达「tile 编号 N 起」，但
实际传递路径是 `win[0x18]`，chs_tile_num 又加了 `win[0x16]` 抵消了 1——
`win[0x16] + win[0x18] = 1 + (chs 想要的 tile 起号)`，永远 = chs 想要的 + 1。
**v4.1 的修法：让 chs_tile_num tm1 完全不依赖 win[0x16]，off = 完整 tile 索引**。
这是反汇编/反算「概念到实现」断层——以后任何"虚拟基址"改造必须**追到
真正算 tile 的那行代码确认**才生效，不能凭 chs_blit 调对了就算完。
