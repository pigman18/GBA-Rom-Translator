# 2026-08-31

## 调研定案：美版 PokeRS 工具架构 vs 本项目路线（重构方向再简化）

读完 `tools/Pokemon_GBA_Font_Patch/pokeRS` 全部 hook 源码 + pokeruby 美版 text.c：

1. **美版工具核心 hook 只有一处**：`DrawGlyphTiles+2 → bl DrawGlyphTilesChinese`，加 GetGlyphWidth/GetStringWidth 两个宽度 hook。对战血条（UpdateNickInHealthbox 等 3 处）、寄放系统（PrintDisplayMonInfo）是"不走标准打印器的窗口"逐个专用 hook。
2. **三代引擎字体根本不进 VRAM**：DrawGlyphTiles = ROM 字体 → 窗口 tileData 缓冲（EWRAM）的像素拷贝。官方窗口系统（GetCursorTileNum/window tilemap）本身就是图集分配器+落址系统。16px 全角字符每字独占 2×2 tile，永不共享/踩踏。
3. **字库整体替换 ROM 数据**：PokeRSFontChsNormal/Small 各 0xE0000 字节（~900KB/份），ROM 扩容 32MB。索引→地址是纯函数（(码-修正)<<8|第二字节 <<7），零可写状态。
4. **编码借三代全角双字节区**（首字节 0x01-0x1E + 第二字节），中文槽位 7168+。
5. **结论**："美版可以日版不可以"是伪命题——卡住本项目的不是日版，是自选的 12px 半格 + VRAM 动态分配。16px 满格 + ROM 常驻字库 + 三 hook 路线连自研图集分配器都不需要（官方窗口系统即分配器）。
6. 重构方案随之再简化：渲染层 = DrawGlyphTiles hook + 宽度函数 + 特殊窗口专用 hook；字库 ROM 化；保留 F9/PhraseTable/SLT2 文本编码层与打包链路；tm1 预渲染窗口按美版"专用 hook"思路逐个收敛，废弃通用 zones 机制。
7. D 商历史做法（网络检索佐证）：本质是"套字库"——只动数据不动引擎（字库 tile 替换 + 码表映射 + 文本导出导入 + 图形文本重绘），代表作如 Tom_C《宝可梦金·银》韩版汉化（2021-2024）。

## 用户纠偏（重要）：D 商不是 16px，是 8px 瘦小字体

- D 商经典做法：汉字塞进**半角 Latin 字库槽**（8px 宽、单字节编码），走官方可变宽度通道。字号不是关键。
- **关键验证（pokeruby text.c:3877 DrawGlyphTile_UnshadowedFont）**：官方 blit 是**逐像素 RMW 混合写入**——`sGlyphMasks[width][startPixel]` 支持任意宽度+任意亚 tile 起点，`startPixel+width>8` 时原生写下一列（buffer+=8），mask 外保留已有像素。可变宽度/跨列共享 tile 是引擎原生语义，tile 从不"属于"任何字形。
- **修正后的分界线**：不是 16px vs 12px vs 8px，而是「混合写入（官方语义）」vs「tile 独占分配（本项目发明的槽位机制）」。字号是自由参数（8/12/16 都能走官方通道）。
- 本项目 BUG 根源收敛为一句话：用"槽位分配+tilemap 引用改写"替代了官方的混合写入，引入了数据本身没有的"独占"语义 ⇒ 踩踏/相位/pass2 全是给这个错误语义擦屁股。
- 修正后架构不变量：**运行时零 tile 分配**。动态文本 = DrawGlyphTiles 层 hook + blend 原语 + 官方游标 + ROM 字库；静态预渲染文本 = 构建期烘焙 tile 数据（D 商重绘图片思路），不走运行时槽位。

## 设计稿已落地（给团队其他人看）

- **`docs/REWRITE_DESIGN_混合写入架构.md`** v1 已写入并挂入 `docs/START_HERE.md` §3.6。内容：证据链、日版差异（无宽度函数 → **FontFuncTable 表项重定向**是日版版解法，表@0x081BB3AC 是数据）、blend_glyph 原语规格（返回列数=游标推进唯一依据）、删除清单（ChsPitchCtrl/Slots/PageCurTab/SlotTableVMA/kOptRows/zones/chs_slots/gen_tm1_slots 链）、保留清单（F9/PhraseTable/SLT2/打包链/relocate/字库@0x09xxxxxx）、5 步实施计划、5 BUG+回归验收底线。
- 实施期待定歧义（已记账不阻塞）：美版 GetGlyphWidthChinese 返回值 4/2 的单位 vs 日版处理器硬编码步进的换算，实施期实测定死。
- 下一步 = 设计稿 §5 步骤 1：blend_glyph C 实现 + Python 对拍单测。

## 重构开工：v4→bak，text v5 步骤 1 落地（混合写入架构）

用户定流程：现有 text 整体入 bak/text-v4（configs 与 work 双份，逐位核验一致），
src/text 清空写新 text；reference/ 不再参与构建（vendored 官方文件仅测试对拍引用）。

- 删除：PrintNextChar_hook / text_render / text_layout / text_scene / tile_alloc /
  chs_slots*.inc / entry.s / hook_origin.s + include 四个 text 头。F9 层
  text_translater.c 回填保留（include 从 text_render.h 改 blend_glyph.h）。
- 新增 include/blend_glyph.h + src/text/blend_glyph.c：1bpp/2bpp 两入口。
  🔴 官方 mode0 窗口 tileData 物理右邻=+64B、mode2 血条缓冲=+32B ⇒ spillTile
  必须显式传参（设计稿 §4.3 初版"单指针+8"是错的，已修订）。
  🔴 官方 Width3 特化函数实际展开 4 像素（pret "XXX: why 4?"）——1bpp 路径照抄
  保逐位等价；2bpp 不复制。
- tests/test_blend_glyph.py 三层对拍全绿：C↔官方 vendored（shim 头
  tests/host/text_render.h 提供结构体；mask 表直接从 vendored 源码解析防转录错）
  2000 例逐位一致；Python↔C 1016 例（(fmt,width,sp) 144 组合全覆盖）。
- copy_tile32 收进 game.h static inline（F9 层 GetGlyph 也用）。
- build.bat（两份同步）：text 段只编 blend_glyph + text_translater；
  reference 两条编译行删除。REM 保持全 ASCII。
- 🔴 当前 hook 链接故意红：缺 PrintGlyph/DrawGlyph（= 步骤 2 要写的新引擎渲染
  路径，FontFuncTable 重定向）；EngineEntry 符号随旧 entry.s 消失。
  下一步 = 设计稿 §5 步骤 2。**打 ROM 前必须完成，勿在红链状态下打包。**

## 步骤 2 落地：FontFuncTable 重定向 + 新渲染路径（v5）

- game.h：CHS_GLYPH_ADVANCE_PX/CHAR_HEIGHT/LINE_FEED 12/12/14 → 16/16/16
  （v4 12px 相位注释块整体替换为 v5 16px 整格说明）。
- 新增 src/text/text_render.c：PrintGlyph mode0 = 16px 整格官方模型复刻
  （解压 128B 容器 [TL][BL][TR][BR] → CopyGlyph2bppTo4bpp_Origin 颜色重映射
  15→C/14→E/0→D **直写 VRAM**（官方链路本就直写，FONT_12PX 文档的 IWRAM
  暂存警告只针对自写代码）→ 每列 UpdateTilemap_Origin + cursorTileX++ →
  TILE_OFFSET+=2/列）；tm1/tm3=cursorTileX+=cols、tm2=win[0x20]+=cols*0x40
  消费推进（步骤 3 逐窗补像素）；DrawGlyph=SYM mode0 自绘 8px 单列
  （SYM 编码即 JP PCS 同码，非 mode0 回退原生画半角形）/≥0xF7 消费/其余
  FontFunc_NativeDispatch。
- 新增 src/text/fontfunc_hook.c：FontFuncTm0..3_Hook 四 thunk =
  TranslateHandleChar ‖ NativeDispatch(直调 Origin 地址)。🔴 防递归铁律：
  原生分发严禁经 FontFuncTable（表项已指向我方 thunk）。
- 新增 src/text/hooks_origin.s（P25）：.org FontFuncTable 写 4×.word Hook|1，
  恰 0x10B 止于 FontSubTable@0x081BB3BC（二级表不触碰）。
- main.asm：include 改 hooks_origin.s，头注释更新；JP2CHS_Entry 仅存
  bin 基址标签。build.bat：加编 text_render/fontfunc_hook + 链接 +
  game_syms 发射列表 EngineIwtdEntry → FontFuncTm0..3_Hook。game.ld 去
  ENTRY(EngineEntry)。text.h 增 FontFunc_NativeDispatch 声明。
- 验证全绿：hook build.bat OK（game.bin 5292B）；armips OK（configs 目录
  需从 work 镜像补 charmap.txt + graphic/，属生成产物）；output.gba
  0x081BB3AC 四表项=0x08800d99/dbd/de1/e05 精确命中，二级表原样；
  game.bin @0x800000 逐字节一致；thunk 反汇编确认 TranslateHandleChar
  先行 + 尾调 NativeDispatch(0x08003569)。
- v4 废止面：P01（PrintNextChar 整替换）/ P05（箭头跳板）/ P24（分区器）
  不再存在——官方状态机自跑控制码，无相位状态可同步，中文 tile 不踩
  预渲染字库。v4 的 DrawMenuCursorEF 固定槽方案随之不需要（原生 0xEF 直画）。
- 镜像已同步 work/POKEMON_RUBY_AXVJ00/build；--seed-only 离线打包中。
- 打包完成 + 自检全绿：2536/2536 文本注入、ROM 对齐 32MB，产出
  `roms/outputs/POKEMON_RUBY_AXVJ00_translated.gba`；
  check_rom_hook.py 逐字节一致=True。⚠该脚本 v4 槽位表解析在 v5 下
  崩溃（chs_slots.inc 已删）——已最小修补（文件缺失时跳过，核心
  game.bin@0x800000 比对保留）。成品 ROM 复核：FontFuncTable 四表项
  0x08800D99/DBD/DE1/E05 精确命中、FontSubTable 原样未动。等用户实测
  mode0 对话场景后再 commit。
