# 2026-09-02

## V6 tile 分配器：配置驱动重构（feature/text-v6 分支）

用户纠正了 v6 配置模型，落地「场景规则表 + 确定性 tile 分配器」：

- **V6SceneRule 字段（PrintNextChar_hook.c）**：`tpl`(win地址) / `zones`+`zone_n`(样式 <8→16px,>=8→12px；NULL→默认12px) /
  `avoid`+`avoid_n`(避让带，V6Interval[lo,hi)，gdb_patcher 抓取) / `spacing`(tile间距，0→V6_SPACING_DFLT=32) /
  `band_lo`+`band_hi`(自由带，0→默认值 cb2 [0x30,0x1C8)) / `row_tab`+`row_n`(tile列表，NULL→算法算)。
- **确定性自动分配器**：`v6_next_free_run`（跳过 avoid 并集的空闲连续段扫描）+ `v6_auto_row_base`（按行顺序、每行占 spacing 段、行间留 spacing 间隙）。
  纯函数、每次重算同结果 ⇒ 重绘幂等、**无需 EWRAM 缓存**（freestanding 镜像无 .bss/.data，可写 static 会落 ROM）。
- **调用点修复**：`print_glyph_px` 与 `chs_place` 两处由 `if (rule && rule->row_tab)` 改为 `if (rule)`，使 row_tab=NULL 时也能走自动路径。
- **option 窗口**：`kOptRowBase[7]` 仍写死（暂用于测试）；已补 `kOptAvoid`(cb2 保留区 [0x1C9,0x1F7]/[0x1E0,0x1FF]) + spacing=32 + band[0x30,0x1C8)。
  ⚠ option 首行宽≈90 tile，超出均匀 spacing 容量 ⇒ 必须保持写死；自动算法适合行数少/行宽相近的窗口。
- **编译**：hook build.bat 在 cmd.exe 被安全策略拦截，已改 bash 原生复刻（arm-none-eabi-gcc 在 PATH）。编译+链接通过，map 与 bin 时间戳一致。
- 设计文档 `docs/V6_tile分配器_配置设计.md` §5/§6 已更新（修正"avoid/spacing 已加"的误记，标注双路径已实现）。

## scene.c 拆分（option 配置独立，2026-09-02 后续）
- 用户要求把 option 等场景配置抽到独立 scene.c（"以后这类配置都放这里"）。
- 新建 `include/scene.h`（V6Zone/V6Interval/V6SceneRule 结构 + V6_FREE_LO/HI/SPACING_DFLT 宏 + 函数声明）+ `src/text/scene.c`（kV6Scenes/kOptRowBase/kOptZones/kOptAvoid 配置实例 + v6_scene_lookup/font/next_free_run/auto_row_base/row_base）。
- PrintNextChar_hook.c 删除结构定义/配置/规则函数，改为 `#include "scene.h"` 调用 extern 符号；顶部"设计原则"注释同步修正（09-01「否掉声明式配置」→ 09-02「场景规则表驱动」）。
- 两份 build.bat（configs/hook + work/build）编译段+链接段加 scene.c；work 镜像源码同步（PrintNextChar_hook.c 更新 + scene.c/scene.h 新增）。
- 编译验证：bash 原生复刻 hook 编译+链接通过（map/bin 时间戳一致，v6_scene_* 符号进表，无重复定义、无缺符号）。

## GRID 接入 v6（2026-09-02 深夜，用户令：写死 row_tab 实测渲染仍烂 → 先把 v4 的 grid 接过来）
- scene.h：V6SceneRule 加 `use_grid` + `remaps/remap_n`（V6Remap{lo,hi,alt}）。
- scene.c：忠实移植 v4 `scene_remap_tile`（全局菜单光标/UI 图标保留带 + 每窗 remaps）
  与 `v6_scene_grid_tiles`（官方 mode2 公式 idx = tileBase + (CURSOR_X+tile_x) + (CURSOR_Y+CURSOR_TILE_Y)*30 + origin，origin 仅 tpl[1]==2 给 CHS_MODE2_ORIGIN_SHOP；lower=+30）。
- PrintNextChar_hook.c：新增 `print_glyph_grid(win,rule,g128,ink)` 整格渲染（ink≤8 单列，
  否则两列；像素写 upper/lower，UTM 值 (upper,lower) 配对，cursorTileX 每列+1，不推 TILE_OFFSET）。
  分派点两处：print_glyph_px 开头（覆盖 draw_jp_glyph/DrawHalfWidth/12px 分支）+ chs_place 早分派（覆盖 16px 行基址分支）。
- **option 配置未改**（仍线性 row_tab 写死）——v4 里 option 本来就是线性非 GRID；GRID 是
  SUMMARY 等网格窗的机制。要在某窗启用 GRID：该窗规则加 `.use_grid=1`。
- hook 编译链接通过（bin 8556B）；work 镜像已同步。未跑完整打包流水线（cmd.exe 被拦）。

## 🔴 option 替换BUG根因 = V6Zone 丢了 v4 的 zone.off（2026-09-02 深夜，截图实锤）
- 症状：标签列显示标签+候选混合字形（「快 配 速 度」「打到底！则」）、候选三列互叠、
  选中行红色变黑（红 blend 的 tile 被同行其它黑字块覆盖 = 症状非独立 BUG）。
- 根因：v4 kOptZones 每区有 off（A=0/B=12/C=22，行带 32 tile），同一行 4 个独立
  打印会话（标签+A+B+C）各从自己 off 起写；v6 V6Zone 只有 font_px ⇒ 四者全从
  row_base+0 起写 ⇒ 互覆。GRID 开关与本病无关（option 是 tm1 线性窗，GRID 公式
  对它本来就是错的几何）。
- 修复：V6Zone 加 `off`（行内 tile 偏移）+ `row_tab`（zone 级 tile列表覆盖）。
  标签列 16px（4字=16 tile）行带装不下 ⇒ 独立基址 kOptKeyRowBase[7]
  = 0x141..0x1A5（步进20，cb2 自由带尾段，⚠未 gdb 逐 tile 验证，撞引用字形需重采）。
  tile = zone.row_tab(或主表) + zone.off + (px>>3)*2。
- 文件拆分（用户令）：scene.c = 纯配置（每窗独立注释块），方法全在新建
  src/text/engine.c（lookup/zone/font/row_index/row_base/next_free_run/
  auto_row_base/remap/grid_tiles）。scene.h 导出 extern kV6Scenes[]/kV6SceneN。
  两份 build.bat 加 engine.c。编译链接通过（bin 8948B），work 镜像已同步。
- option 配置 use_grid 已关（tm1 线性窗）。GRID 留给真正的 mode2 网格窗。

## 22:36-02:04 颜色码覆盖 BUG 修复（第二轮，接 gdb 日志）
- 根因（日志+ROM 逐字节实证）：日版菜单行字符串表 @0x37B560 每行自带 `fc 05 0f`（灰），选中时官方原位改写 `fc 05 08`（红）；我们的候选词做成 F9 短语后短语体内残留 `\CC050F`，打印时把菜单改好的红色盖回灰（选中会话 UTM pal=0xF，LR 直印行 pal=0x8）。
- 修复：texts_translated.json 中原文/译文均以 \CC050F 开头的 12 条候选词，剥掉译文开头冗余颜色码（原文保留）。
- 完整流水线重跑成功：POKEMON_RUBY_AXVJ00_translated.gba (02:04)，check_rom_hook game.bin@0x800000 逐字节一致。
- MEMORY.md 超限已清理重写（~3.5K chars）。

## 03:16-03:25 颜色码 BUG 治本：texts_patcher 加 IgnoredReader（源头上剥 FC 前缀）
- 用户令：颜色码覆盖 BUG 不在渲染层修，而在导出层源头解决——texts.json 的 UI界面
  模块加 `ignored_reader`，从源头上把行首 FC 控制码前缀剥离。
- **权威语义澄清（bulbapedia Gen III 编码表）**：`fc 05` 是「改调色板」命令（1 参数），
  **不是颜色码**；颜色命令是 FC 0x01(文字色)/0x02(高亮)/0x03(阴影)/0x04(三色一体)。
  `fc 05 0f`(灰)/`fc 05 08`(红) 改的是调色板号。`fc 08` 是「暂停」命令。此前记忆里
  「FC=颜色码」的认知是错的。
- **IgnoredReader（texts_patcher.py）**：value 全枚举 `\CC…` 前缀，剥字节数 = 1(fc) + hex位数/2，
  不做任何命令/参数语义推断（用户明确「统一语义，都是 \CC0509 这样子，别发明第二套解析」）。
  strip 同步剥 original 文本 + original_hex 字节，保证两侧对齐；只剥行首，句中不动。
- **配置 src/util/configs/POKEMON_RUBY_AXVJ00.yaml**（⚠ 真正的导出配置源头在此，不是
  configs/translate/texts.json）：UI界面 模块 `ignored_reader` 全枚举 5 个调色板前缀
  `\CC0505`(战斗菜单)/`\CC0508`(空行占位)/`\CC0509`(设置菜单+制作表)/`\CC050E`(剧情)/`\CC050F`(选项行)。
- **YAML 坑**：单引号标量 `'\\CC0505'`（双反斜杠）会解析成 `\\CC0505`；要表示单个反斜杠须写 `'\CC0505'`。
- 验证：导出 UI界面 511 条目，行首 \CC05 前缀残留 0 条；`おそい` 的 original_hex 从
  `fc 05 0f 05 0f 02 ff` → `05 0f 02 ff`。slot 匹配 key 变纯假名，FC 前缀留给官方原位改写灰↔红。
- 提取文本控制码分布（texts.json 全量）：\CC05 66次/08 103次/09 77次/0C 46次 是高频；
  另有大量 \CC69/\CC92/\CCC4/\CCFF 是扫描器把非文本字节误解码的垃圾，非真实控制码。

## 03:44 IgnoredReader 扩展：整串前缀 + 剥空→null 过滤
- 用户令：UI界面、补漏剧情两模块 ignored_reader 都加 `\CC050FＬ\CC0CFBＡ`（L/A 键位提示整行，
  0x837B5BD，字节 `fc 05 0f c6 fc 0c fb bb`）和 `\CC0508`（空行占位，0x837B5AB）。
  这两条无翻译价值；reader 语义：value 项在行首匹配截断，**截空则返回 null 整条过滤**。
- **改造（texts_patcher.py IgnoredReader）**：
  - value 元素从「纯 \CC+hex 前缀」扩展为「任意完整前缀串」（含全角字/嵌套 \CC 控制码），
    字节由 `encode_pcs()`（meowth.runtime_cache）精确还原；text 用 `.upper().startswith` +
    raw 用 `.startswith(bytes)` 双重匹配；前缀按字节长度**降序**排序（长整串优先于短 \CC050F）。
  - `strip()` 返回值改为 `tuple[str,bytes] | None`：截断后 `text.strip()` 为空 → None。
  - 4 处调用点（普通 scan / msg fallback / msg 命中 / 择优）改判 None：普通 scan 直接 return，
    其余 `a=end+1; continue`；msg fallback 把 fb_text 置 "" 走 falsy 分支跳过。
- 验证：单测 4 用例全过（整串→None / 空行→None / 前缀+正文→剥前缀保留正文 / 无前缀→原样）。
  完整导出 12697→12695 条：0x837B5BD 与 0x837B5AB 已过滤，0x837B5B7（\CC050FＬＲ）保留剥前缀剩 ＬＲ。
- 安全边界：\CC0508 带正文的垃圾块（0x834E05D おくおかかく 等）剥前缀后仍有正文，不会被 None 误伤。
