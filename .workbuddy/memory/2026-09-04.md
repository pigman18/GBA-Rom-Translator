# 2026-09-04 工作日志

## 设置菜单左 16px / 右 12px 混排落地（完成编译打包，待实机验证）

用户需求：设置菜单（模板 0x081BB874）左边 key 显示 16px、右边 value 显示 12px，
配置方式由我定，v4 作参考（能复用就复用，不能就重写）。

### 决策：不搬 v4 PTR 固定槽机制，复用 v7 已有的「按 curX 分字号」机制

理由：v4 的 PTR 槽表（chs_slots.inc 41 字 + scene_note_glyph per-glyph 绑定 +
两趟 refpr 落址 + RAM 传 scene_ptr_base）是侵入式大改，且「改翻译要重新生成槽表」
违背 v6 去登记化方向。当前 v7 已具备完整 16px 整格落址路径（chs_place 的
fontSize==16 分支 + V6Zone.row_tab zone 级独立行带字段），只需改数据配置即可。

### 改动（都在 src/text/PrintNextChar_hook.c）

1. **新增 kOptLabelRowBase[7]**：标签列独立行带，7 段 × 16 tile，选自 cb=2
   空闲缝（避开 kOptGlyphAvoid 26 项 + 候选行带 + 高段 [0x1C8,0x1FF]）：
   `0x003/0x053/0x079/0x0ED/0x15F/0x173/0x19B`。
2. **kOptZones[0]（标签列）font_px 12→16 + row_tab=kOptLabelRowBase**。
3. **候选列 off 重排 12/18/24 → 0/10/20**（标签独立后候选独占原 32-tile 行带，
   12px 相位共享 3 字 = 10 tile，间隔 10）。
4. **修 v6_same_zone 加 font_px 判据**（关键）：16px 标签列 off=0 与 12px 候选 A
   off=0 相同，旧判据只看 off ⇒ 候选列被误判为「标签列同行后继块」→ 相位不复位
   → 落址错位。加 `&& font_px==font_px` 后，字号不同 = 不同区，正确复位。

### tile 空间核算

cb=2 自由带 [0,512)（charBlock2），空闲 ≥16 tile 的连续段 166 tile ≥ 需 112 tile
（7×16）。标签行带 16px 每字 4 tile，最长「对话速度」4 字 = 16 tile/行。
候选 12px 相位共享 3 字（单声道/打到底/立体声）= 10 tile。

### 相位隔离正确性

标签 16px 与候选 12px 分属不同 zone（font_px 不同）→ v6_same_zone 判异区 →
chs_init_phase（InitTextPrinter hook）正确复位 px，不互相续接。

### 流水线（全绿）

- hook 编译链接：bash 复刻 build.bat 全步骤，game.bin 8060 字节（+8B 来自新数据表）。
- game.map 无 .bss/.data 落 ROM（0x0），无软件除法。
- 根打包 --seed-only（bash 直跑，PYTHONPATH 用 Windows 路径）：9510 replaced、
  ROM 对齐 32MB。
- 产物 POKEMON_RUBY_AXVJ00_translated_new.gba（原名被 mGBA 锁定，写 _new）。
- check_rom_hook.py 对新 ROM：`game.bin @ 0x800000 逐字节一致 = True`。

### ⚠ 待实机验证（铁律：没看到截图符合预期前只能是假设）

- 标签列 16px 是否显示完整、无撞 tile（kOptLabelRowBase 的 7 个 tile 号是我
  静态算的空闲缝，kOptGlyphAvoid「清单不完整」是主要风险，可能有未记录的官方
  引用字形撞上）。
- 候选列 12px 是否正常（off 从 12/18/24 改 0/10/20 后切换值不覆盖）。
- 三个旧撞 tile 问题（声音缺字/关闭重复/属性7）是否缓解。

### 打包姿势备忘

- PowerShell 5.1 跑 `& "C:\Python314\python.exe" ... | Out-File` 报
  「CantActivateDocumentInPipeline」；改用 **bash 直跑** `PYTHONPATH="C:\..." /c/Python314/python.exe -m meowth full ... --seed-only` 最稳。
- check_rom_hook.py 支持 `sys.argv[1:]` 传 ROM 路径，验证 _new.gba 用：
  `python scripts/check_rom_hook.py roms/outputs/POKEMON_RUBY_AXVJ00_translated_new.gba`。
- ⚠ **PowerShell `& .\build.bat *>&1 | Out-File` 会静默吞输出且不真正重编**（log 0 行、
  game.bin 时间戳不更新）。判定是否真编了：`stat -c %y out/game.bin src/...c` 对比
  时间戳。**改源码后先确认 bin 时间戳晚于源文件**，否则是旧包。稳妥：bash 直接复刻
  gcc 步骤（工具链在 bash PATH 里：/c/soft/env/arm-gnu-toolchain-15.2.../bin）。

## 标题「设置」被「对话速度」覆盖 BUG 修复（2026-09-04 第二轮）

### 症状（用户截图反馈）

16+12 混排 OK 后，标题行仍撞：应显示「设置」，实际显示「对话」（= r1 标签
「对话速度」的前 2 字）。

### 根因

`v6_scene_row_index()` 把 `curY <= row_y0(3)` 一律 clamp 到行 0。标题 curY=1 与
r1 标签 curY=5（(5-3)>>1=1→行0）都映射到行 0 ⇒ 共用 `kOptLabelRowBase[0]=0x003`。
标题「设置」2 字（8 tile @0x003..0x00A）被 r1「对话速度」4 字（16 tile @0x003..0x012）
覆盖 ⇒ 标题显示成「对话」。

### 修复（v7 简单行带方案，不给标题复用 r1 行带）

1. `struct V6SceneRule` 加字段 `uint16_t title_base`（标题行专用 tile 基址，0=无标题）。
2. `kV6Scenes[0]` 设 `.title_base = 0x183u`（cb=2 空闲缝，紧跟 r6 标签带 0x173..0x182
   之后，避开全部标签/候选/引用字形/高段；标题 2 字 16px = 8 tile，0x183..0x18A 够）。
3. `v6_scene_row_base()` 顶部加：`if (cy <= r->row_y0 && r->title_base != 0u) return r->title_base;`
   （标题不再 clamp 到行 0 与 r1 撞）。

### 验证

- 反汇编确认 title_base=0x0183 已编进 kV6Scenes（.text @0x08801b50 起 `...07 00 83 01...`）。
- game.map 无 .bss/.data、无软件除法。game.bin 8052 字节（-8B：title_base 分支改了结构）。
- ⚠ 仍待实机截图确认「设置」正常显示且不与 r1 撞。
