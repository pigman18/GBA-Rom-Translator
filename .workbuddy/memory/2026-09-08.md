# 2026-09-08

**作者**：Auto（Cursor）/ Composer

## 午前：Middle「号」错乱 — CY2==游戏；真根因=nibble 端序

### 用户输入
- 图：CY2 里 bin「号」错乱、游戏「9号」同错、Middle.bdf「号」完整。
- 纠正：别信 memory 里「CY2 摆法伪影」；**CY2 显示与游戏一模一样**。
- 再补图：CY2 @偏移 `1F400`、宽8 高16、GBA 4bpp，网格里「号」与其它字同系扭曲 → 生成 bin 逻辑有问题，勿纠结地址。

### 排除 / 误判链
1. **GLM「CY2=查看器伪影」**：错误（用户钉死 CY2==游戏）。
2. **跟风「tile 序 TL,TR,BL,BR vs TL,BL」**：错误。`pack_slot16_4bpp` 与 GetGlyph/`chs_rasterize` 同为 **TL,BL,TR,BR**；按列序+「高 nibble=左」解包，「号」与 BDF 墨迹一致。
3. 「BDF→bin 6807 全等」假闭环：cmp 用了与错误 packer **相同的高=左**约定，自洽不等于 GBA/CY2 正确。

### 真根因
- `scripts/build_chinese_font.py` `pack_slot16_4bpp`：`(left<<4)|right` → **左=高半字节**。
- 标准（GBATEK / CrystalTile2 GBA4bpp / `blend_row_4bpp` / `extract_cols`）：**左=低半字节**。
- 结果：相邻像素对调 → CY2 与游戏同乱；BDF 源正确。
- 2026-08-31 MEMORY 已写「注释 left=high 过时、字库应低=左」，**代码未改到本次**。

### 已修（程序，未代打 ROM）
- pack：`(right<<4)|left`；注释改为 GBA 低=左。
- 同步解压/预览：`engine._decompress/_compress_4bpp_glyph`、`sim_gba_font_blit`、`font_panel`、`fonts/font_tools/view_font_chs.py` + `check_font_layout.py`。
- 重生成并同步 unshadow：`work/POKEMON_RUBY_AXVJ00/graphic/fonts/PokeRSFontChs{Middle,Normal,Small}(_unshadow)(0xE0000).bin`。
- 验收（静态）：新 Middle「号」按 **GBA 低=左** 解左列 8×16 == BDF narrow 墨迹。

### 铁律补记
- CY2（GBA4bpp）== 游戏像素 ⇒ 信数据/生成/绘制，不甩锅查看器。
- 比对 BDF↔bin 必须用 **GBA nibble 序**解包，禁止用 packer 错误约定自证。
- tile 容器仍是 TL/BL/TR/BR；本次只修 nibble，不改象限序。

### 待用户
- 说「打 rom」后嵌入新 bin，CY2 + 实机再看「号」/粉框「119号道路」。

## 2026-09-08 凌晨2：方案 A 首版实现 + 打包交付【未实机验证】
- 用户指令「按你的想法实现，完成后执行流水线给测试 ROM」。实现 tile_alloc.c v9（方案 A 首版安全子集，未做 BGxCNT/模板重定向）：
  ①cb0 两级区间：优先「atlas 上方独占带」[0x2D2,0x400)（官方 tm1 字库固定止于 0x2D1，其上无可静态指认的官方固定产出=可声明所有权带），耗尽回落 [0x100,0x2D2)；其他 cb 单区间原行为。
  ②ours-GC：v8_alloc_begin 末尾把「本 cb 段表记录 ∧ 不在活引用位图」的历史 glyph 段 32B 清零释放，段目清空。治「负缓存只避让不回收→非空 tile 随会话单调累积→空间越用越少」的机制性根源。
  ③段表 v9 编码：段带 charBase 标签（bit10~14），修掉跨 cb 相对号错回收隐患；magic 0xA5C3→0xA5C4 使旧表失效。
- 探测层（活引用/非空/段表）保留为 tripwire，发现声明失效自动绕开。渲染路径零改动（场景表保持「屏蔽」提交后的状态，未动）。
- 构建：bash 手复刻 gcc 序列 15 obj 零警告 → game.bin 9384B（旧 8968B）→ meowth full（venv python，PYTHONPATH 需 Windows 反斜杠格式）→ check_rom_hook.py ROM@0x800000 逐字节一致=True。
- 交付 roms/outputs/POKEMON_RUBY_AXVJ00_translated_new.gba（旧名 translated.gba 被 mGBA 映射锁定 WinError 1224，需用户关模拟器后改名）。
- 遗留（方案 A 后续）：cb2/cb3 容量天花板需场景→空闲块分级表 + 模板重定向（无场景 gdb 数据，本轮不做）；半角字符同块化仅在重定向路线才需要。

## 2026-09-08 凌晨3：v9 实机证伪 → 回退 v8 基线重新打包
- 用户三张实机图（地图花屏团/summary 日文/设置页撞 UI 后左右失效）证伪 v9 两处结构性改动：
  ①cb0「独占带」[0x2D2,0x400) 被地图 tileset 使用——官方走出/走进地图时 DMA 新图块进 cb0 高段，与我们 glyph 双向踩踏（图1 越走越乱的根源）；
  ②ours-GC 在共享区清零=主动擦官方数据（v8 从不写非己数据的安全方向被 GC 反转）。
- 共性根因（与 CY2 事件同模式）：把「测出来现在空」当「永远没人写」的所有权声明，未做场景→空闲块分级表（方案 A 文档里的「地基」）就跳到了实现。
- 修复：git checkout HEAD -- tile_alloc.c（回到 2026-09-07 实机验收通过的 v8）→ 15 obj 零警告 → game.bin 8968B、game_syms.asm 与 HEAD 逐行一致 → meowth full 成功写主文件名 → check_rom_hook=True。
- 交付 roms/outputs/POKEMON_RUBY_AXVJ00_translated.gba（02:39，32MB 对齐版）。
- 图2 summary 页日文标签（もちもの/リボン/のうりょく/けいけんち）=「屏蔽」提交停用场景表后的翻译覆盖问题，与分配器无关，另行处理。
- 教训：方案 A 的场景级 charBase 重定向必须先有逐场景 gdb 实测的空闲块表，任何「静态声明独占」在没有实测数据前都是赌博。

## 2026-09-08 凌晨4：v8 基线两处新撞 UI（队伍血条 / 华丽大赛详情页）→ 静态分析定案
- 用户实机图①：队伍页五条血条同一相对位置同伤（右上 HP 标签区红墨团 + 右缘图标列）→ 共享 healthbox 图形 tileset 的 1~2 个 tile 被我们 glyph 叠印；图②：华丽大赛技能详情页右上角 B 键 UI 被叠。图②另有日文标签（けりそ…/せつめい）=「屏蔽」提交后的翻译覆盖问题，与分配器无关。
- 静态定位：UpdateNickInHealthbox/UnusedPrintMonName hook 只改文本流不做分配，中文字形均走 chs_print→v8_alloc_tile；场景表现仅 kOptionScene，血条名 tpl 40C 与详情页均走默认 12px。损伤机制候选：a) begin 时 healthbox BG/图块尚未就绪→活引用扫描看不见→声明后被官方图形占用叠印；b) tile 曾是我们 ours→官方复用后 v8_in_ours 判可回收→blend 叠官方图形。两候选均需 gdb 实测区分。
- 采集方案（分级表地基，一次采两场景）：mGBA Qt 开新 ROM + Start GDB stub → `python src/util/gdb_patcher.py log --functions ChsPrint,V8Alloc,UpdateTilemap --ui-survey` → 队伍页滚动 + summary 华丽大赛页切换 → 停。UISURVEY 每场景 dump DISPCNT/BGxCNT×4/tilemap 引用桶/cb 非零区间/OBJ 位图/OAM；V8Alloc 记领号；UpdateTilemap 记落格。埋点地址 0x088007B4/0x088011E4 与回退后 HEAD 构建吻合（map 已核对）。
- 纪律：未拿到这两场景实测数据前，不再改任何分配区间——静态声明独占已被证伪两次（CY2 事件、v9 独占带）。

## 2026-09-08 凌晨5：pokeruby GetCursorTileNum 源码判读完稿——官方无回收机制，无物可抄
- 用户指令「参考 pokeRS 的 GetCursorTileNum 底层，看回收 tile 重新分发机制能不能抄」。克隆 pret/pokeruby tarball 至 $TEMP/pokeruby-master（git clone 被拦，curl codeload 可行），text.c 全链读完，判读文档=docs/调研_20260908_pokeruby_GetCursorTileNum机制判读.md。
- 源码实锤：GetCursorTileNum（text.c L4380）=纯算术 `tileDataStartOffset+tileDataOffset+2*xOffset+yOffset`（等宽）/网格坐标式（变宽），无扫描无探测无回收。分配=窗口创建期一次性预留（变宽 `2+width×height`，等宽字体预载 256 字形=512 tile），调用方顺序拨号（menu.c gMenuTextTileOffset 游标），每字只进不退（AddToCursorX 跨 tile 边界 tileDataOffset+=2），换行复位 2 或 2*sLineLength+2。「回收」仅场景粒度：场景代码重拨基址（TextWindow_SetBaseTileNum/SetDlgFrameBaseTileNum），全区间整体易主；grep 全仓无任何逐 tile 回收代码。
- 结论：官方把分配问题消灭在窗口创建期（拥有全部窗口创建代码+静态预算区），我们注入架构两条前提均不成立（改不了创建流程；预算区 128~256 连续 tile 已实测 2/8 场景选不出）→「抄官方」不可行，与 MEMORY 网格落址结论互证。

## 2026-09-08 上午：CHS_PRINT_FONT_PX → 按 tm 分档宏 + 请求字号贯通 chs_print【未实机验证】
- 用户指令：①CHS_PRINT_FONT_PX 改为 CHS_PRINT_TM0/TM1/TM2/TM3_FONT_PX（各 textMode 默认字号）；②支持 8/12/16px，8px=Middle 库，12/16=现状；fontNum=4 强制 8px Small。
- 实现：text_translater.c 四宏（TM0/1/3=12，TM2=8）+ chs_print_px(win) 按 WIN_TEXTMODE&7 选值，3 个 chs_print 调用点改传；PrintNextChar_hook.c resolve_draw 增 req_px 入参，优先级=fn4 强制 8px Small > 场景表 > req_px（8→Middle lib=2，16→16，其余/0→12）；chs_print 恢复消费 fontSize，DrawHalfWidth/DrawGlyph 传 0（无请求）。默认值全等于现状 → 预计实机视觉零变化（预测：与 v8 基线截图逐场景一致，唯一新增通路=某 tm 配 8 时走 Middle）。
- 坑：并行 Edit 同一文件竞态丢改动（text_translater 3 调用点 + resolve_draw 主体首次未落盘），grep 全量验证后串行重做才齐——多文件同点改动禁并行 Edit 同一文件。
- 构建：15 obj 零警告 → game.bin 9056B（旧 8968B，+88B）；game_syms.asm 需按 HEAD 格式仅输出 map 中存在的 5 符号（batch findstr 找不到会跳过，bash 循环要 if 判空），地址与 HEAD 逐行一致；meowth full 1m26s 完成（翻译全缓存复用，无 LLM 调用）→ translated.gba 32MB 对齐，check_rom_hook.py ROM@0x800000 逐字节一致=True。
- 交付 roms/outputs/POKEMON_RUBY_AXVJ00_translated.gba（2026-09-08 09:36）。待用户实机验收：默认值=现状应视觉零变化。
- 对血条/华丽大赛撞 UI 的指向不变：仍走 gdb --ui-survey 实测 + 场景表扩条路线（kMapInfoScene/kTrainerInfoScene 先例）。

## 2026-09-08 上午：GBA 日版汉化思路全网检索（用户指示「看看有没有能抄的」）
- 四轮 WebSearch，判读文档=docs/调研_20260908_GBA日版汉化思路全网检索.md。业界四路线：A 静态码表+槽位改画（汉化组主流）、B 反编译改造（rh-hideout-chinese/bubble128 中文 fork）、C 工具链化（FEBuilderGBA：Draw utf-8 patch+字库库+缺字自动闭环+headless playtest）、D 自建渲染层（MOTHER3/GBA_MultiMenu NFTR/tonc TTE/本项目）。
- 判读：tile 分配/防撞 UI 痛点全网无现成方案（主流路线不面对此问题），v8 无公开同类，只能继续 gdb 实测+场景表扩条。✅ 可抄三件：①译文字符集自动审计（打包期扫译文⊆字库，缺字不再靠实机暴露，性价比最高）；②headless 实机回归（mGBA 脚本化按键+截图，四步法第③步自动化，mGBA Lua 能力未验证）；③NFTR CWDH 三值宽度表（bearing/墨宽/步进分离，居中排版用，挂账）。⚠ slot/F9 编码、12px 相位合成、LZ 注入均已有对应物不弱于业界；静态路线会毁掉日文回落通路，反编译路线需废弃 v8 全部体系，均否。

## 2026-09-08 上午：译文字符集自动审计落地（FEBuilderGBA glyph 审计模式，调研可抄点①）
- 新文件 src/meowth/glyph_audit.py：audit_translation_glyphs(entries, charmap, game_work, log, game) 以实际产出 bin 为准（空槽=全零128B，不假设BDF来源），slot 映射复用 scripts/build_chinese_font.parse_charmap（importlib 按路径加载）。
- 关键甄别（防误报）：①translated==original 的整条保留日文条目跳过（inject 前已被 _injectable_translation 过滤，不丢字）；②U+3000 全角空格白名单 _KNOWN_BLANK（空槽=渲染正确）；③只审 encode_char 返回 2 字节的双字节字符，单字节走原版 PCS 不审。
- 🔴 实测发现真问题：encode() L394-397 对编不出的字符**静默丢弃**（不 crash 不警告）→ 剧情混排日文假名（シェルダー等宝可梦名残留）+ Ⅱ(多边兽Ⅱ) 共 122 个字符实机直接消失。三库 bin 空槽 0 个 = 汉字字形覆盖完备，缺的全在编码层。charmap 缺码 122（报告 work/POKEMON_RUBY_AXVJ00/glyph_audit_report.txt），是否补码/清洗译文待用户拍板。
- 接入：engine.build_rom 在 inject_texts 前 try 包裹调用（失败仅 warning 不阻断）；CLI 新增 `meowth audit-glyphs [texts_json] --game --work-dir`（缺省 texts_json=work/<game>/translate.build.json——注意 configs/translate/texts.json 无 translated 字段，权威译文源是 translate.build.json）。
- 坑：get_charmap_path 在 config_loader 不在 resource_path；cli.py 需补 import sys。

## 2026-09-08 上午：pokeE 字体绘制机制判读（用户问 9×9/11×11 怎么绘制、省不省 tile）
- 源码 tools/Pokemon_GBA_Font_Patch/pokeE（FRLG 引擎，armips 工程）：两套 1bpp 位流字库 gba_chs_font_11x11.bin（6763 字×16B=108KB）+ 9x9.bin（6763×11B=74KB）+ 各 9 标点小库，GB2312 全集，直接 incbin 进 ROM 扩展区（HackFunctionAddresses）。
- 绘制链：RenderText hook→ChineseCodeCheck（双字节 hi∈{1-5,7-26,28-30} lo<0xF7，符号 0x36-0x3E 单字节；glyphId=hi_adj*247+lo，与我们 charmap 同构）→GetChineseFontWidthFunc（Normal→宽12大库 / FONT_SMALL|NARROW→宽10小库）→DecompressGlyph_Chinese 写 gCurGlyph.width/height（12/10 × 15/13）→Convert1bppTo2bpp（EWRAM 0x0203FF40~FFC0：位流解包 1bpp 16×16→2bpp+右下阴影自动生成 overlap 去除→4 tile 切割）→官方 DecompressGlyphTile×4 写 gCurGlyph→官方 RenderText+0x45C CopyGlyphToWindow 落 VRAM。0x3E 以上双字节 currentChar++。
- 📌 tile 账核心：VRAM tile 号消耗由 **advance（12/10px）** 决定，不由墨迹（11/9px）决定。11×11/advance12 的官方 mode0 账（pokeruby text.c AddToCursorX L2804 跨 8px 边界 tileDataOffset+=2 + DrawGlyphTile_UnshadowedFont L3877 mask 保留合并=sGlyphMasks[width][startPixel] 相位共享）= 我们 12px 现状同账（n 字 n+1 列 ≈ 2n+2 tile 号）；9×9/advance10 约 2.5n+2，省 ~17%。我们 Middle 8px advance8（2 tile 号/字）仍最省。
- 结论：pokeE **不解决 tile/撞 UI 问题**——它没有分配器，完全依赖官方 mode0 窗口网格（窗口预留+亚 tile mask 共享），hook 场景少。真实优势=ROM 空间省 8 倍（16B/字 vs 我们 4bpp 128B/字）+阴影运行时生成+墨迹尺寸自由（改 9/10/11px 不用重打 4bpp）。代价=每字运行时 1bpp→2bpp 转换 CPU（手写 ARM 汇编位流解包）。
- ⚠【推论/未验证】pokeE 落 VRAM 的亚 tile 共享行为由官方 CopyGlyphToWindow（RenderText+0x45C）决定，未实测 FRLG 版该函数；从 pokeruby 同源 mask 机制 + advance12 非整列必然共享推得。若要实锤需 gdb 采 CopyGlyphToWindow 写 tile 号序列。
- 审计报告（122 缺码）用户拍板「暂不处理只看报告」。

## 2026-09-08 上午：领航员撞字根因分析（用户问「美版为啥一页渲染那么多中文都不撞」）
- 核心差异：官方/pokeE 文字永远落在**窗口创建时静态预留的独占网格**（2+width×height tile，pokeruby TextWindow_SetBaseTileNum），tile 号=网格坐标纯算术，空间不够的表现=换行/滚动，物理上不可能撞别人；且官方英文字形 8px 整列 advance，每字恰 2 tile 号，密度编译期可预算。
- 我们 v8：共享 cb 里动态领号 + 12px 跨列拆分（8+4/4+8，相位 0/4），每字 3~4 tile 号（2 列竖直对），密度=官方英文 1.5~2 倍；相邻字共享中间列的前提（同会话顺序渲染）在碎片 cb/跨会话下破裂→尾巴写进别人 tile 或回卷重写→撞。
- 🔴 眼下直接放大器：「屏蔽」提交 ea7507b 把 kPokeNavScene 规则注释停用后，当前 ROM 领航员回退 12px 渲染；而 2026-09-07 验收通过的组合是 kPokeNavScene→Middle 8px（advance8，每字 2 tile，密度=官方英文）。恢复场景表规则（改 scene_cfg.c）+ 重编重打包即可解决领航员撞字，属已验收组合回归。
- 9×9/advance10 仅省 17%（相位需加 2/6 两态），优先级低于 8px 化。

## 2026-09-08 上午：用户拍板换地基——整套换成 pokeRS/pokeE 模型（v8 退役）
- 用户原话：直接整套换成 pokeE 那套；当前工程不是标准项目；该换地基就换地基，大不了回滚。停止一切止血思路。
- 回滚锚点=git 5f70593（v8 验收态+TMx 改造）。蓝图文档=docs/重构_20260908_pokeRS式地基更换蓝图.md。
- 关键新认识：官方网格模型下每行 tile 列消耗=行像素长度÷8，与每字 advance 无关（mask 共享下列）→12px 不爆网格。pokeE 12px 可行的真正原因。
- pokeRS=红宝石上的工作先例：hook DrawGlyphTiles(0x08006874/其ROM版本)，中文双列 4×DrawGlyphTile_ShadowedFont 写 win->tileData 官方网格（GetCursorTileNum 寻址），GetGlyphWidthChinese/GetStringWidthChinese 管宽度。其字库 4bpp 128B=我们现库格式（W5 前不用重打）。
- pokeE text.s 全读完：位流=宽×宽 bit 连续排布（大库 11×11→16B 步进，行偏移 1），Convert1bppTo2bpp=位流→16×16 1bpp→2bpp+右下阴影（重叠去除）→切 4 tile→官方 DecompressGlyphTile(2bpp→4bpp)×4→gCurGlyph（FRLG 0x03002F90，4×32B）。gCurGlyph 布局：+0 上右/+0x20 上左/+0x40 下右/+0x60 下左（FRLG Cut 顺序）。
- 🔴 W1 风险：pokeRS 符号是另一 Ruby 版本，AXVJ00 全套地址必须重订（DrawGlyphTiles/DrawGlyphTile_ShadowedFont/UnshadowedFont/GetCursorTileNum）；AXVJ 无 GetGlyphWidth/GetStringWidth（0822 定论）若属实，宽度链路自建调用点补丁。struct Window 字段偏移（[0x01]fontNum [0x10]cursorX [0x1C] [0x1E]textIndex [0x20]*text [0x24]*tileData）需逐个核对。
- W2 编码对齐：翻译输出改落 pokeRS 双字节区（hi∈[1,0x1E] 除6/0x1B，lo<0xF7，247/带已同构），F9 槽位退役；日文回落=「不编码」天然实现（新地基的意外收益）。
- 下一步顺序：W1 订址 → W2 编码 → W3 三件套移植 → W4 专用 hook 逐场景回迁（验收不过允许暂回日文）→ W5 1bpp 换库（EWRAM 三缓冲落点需实测，pokeE 0x0203FF40~FFFF 与旧占用冲突但 v8 下线后大半释放）。
- 用户补充拍板（换地基架构定稿）：①9×9 小字体也要接过来；②BDF 不满足就直接用 pokeE 的 bin 测（gba_chs_font_11x11/9x9 + PokePunctuationChs，GB2312 双字节编码与 W2 目标区原生一致=编码层零对齐成本）；③整体走大字体（11×11/advance12），fontNum=4 强制小字体（9×9/advance10）——与我们 resolve_draw 现有 fn4 最高优先级触发点一致。scene_cfg 场景字号规则与 Middle 8×12 全部退役，两档制。Convert1bppTo4bpp（pokeE text.s L237-378）并入 W3 移植；大库参数 width=11/行偏移1/高15/宽12，小库 width=9/行偏移2/高13/宽10。W5 改为「自有 BDF 派生 1bpp 位流替换 pokeE bin」。

## 2026-09-08 W1 执行结果（反汇编定论，改变施工方案）
- 🔴 AXVJ00（日文 Ruby）= 等宽 tm1 图集引擎，**无美版 mask 引擎**：sGlyphMasks 特征字节（width0/width1 行 96B 模式）全 ROM 零命中 → DrawGlyphTiles/DrawGlyphTile_ShadowedFont/GetGlyphWidth/GetStringWidth 均不存在。0822 定论正确。pokeRS 目标 ROM=美版（地址表不符 + LANGUAGE_ENGLISH 分支）。
- AXVJ00 新钉锚点（BL 反查脚本 work/POKEMON_RUBY_AXVJ00/find_bl_callers.py + 逐指令比对 pokeruby 源码）：0x36DC=UpdateTilemapHelper(win,upNum,loNum) 写 tilemap[0]/[32]；0x3708=GetCursorTilemapPointer（win[0x1A]+[0x1B]+([0x1C]+[0x1D])*32，win[0]→+0x10=tilemap 基址，win[0x0F]=调色板）；0x3730=GetGlyphTilePointers（fontNum 0-6 七路 switch）；0x33B4/0x3630=DecompressGlyph 双胞胎；0x3830/0x38A0=DecompressGlyphTile 无阴影/含阴影；0x338C=LoadFixedWidthGlyph+推进（**win[0x20]+=0x40/字**，win[0x20]=窗口网格运行游标=美版 tileDataStartOffset+tileDataOffset 的等价物，更简单）；0x34D4~0x35DC=WriteGlyphTilemap_FontX 家族（tm1 图集 tileNum=startOffset+2*glyph）。
- 施工方案改为：**把美版 mask 引擎核心移植进 AXVJ00**（不换基 ROM）——sGlyphMasks 整表照搬（pokeruby text.c L251）+ GlyphBuffer/ApplyColors_ShadowedFont（IWRAM）+ ShiftGlyphTile_ShadowedFont_Width0~8 全家 + DrawGlyphTile_ShadowedFont + DrawGlyphTilesChinese（pokeRS 逻辑、AXVJ 字段驱动：col1=win[0x20]+0/0x20，col2=+0x40/+0x60，写 4 格 tilemap，win[0x20]+=0x80、cursorX+=12/10、textIndex+=2）。fn4→9×9 小字体。
- 待查：AXVJ 自动换行宽度计算点（无 GetStringWidth；FE 注入换行覆盖大部分）；GlyphBuffer IWRAM 落点（构建时定）。

## 2026-09-08 下午：地基更换 W2/W3 实装完成（待实机验收）
- 反汇编补钉（W1 遗留）：0x36DC=UpdateTilemapHelper 只写 tilemap[0]/[+0x40] 两格+调色板，无 0x1A 副作用；0x34E0/0x3500=tm3 网格寻址 tile=TILE_BASE+win[0x1A]+2+win[0x1B]+(win[0x1C]+win[0x1D])*30，数据直写 tilemap 引用格；tm0 writer=0x3520/0x3568（线性 TO+=2）；tm1=0x3584（entry=TILE_BASE+2*glyph 图集，无窗口私有区）；模板实测：对话框×5/地图名/粉框/队伍全 tm1，招式 tm0，血条 tm2，主对话框 tileData=0x06008000(cb2)。
- 🔴 架构定论：AXVJ 无中央 tile_base 分配器（各调用点字面量传入）；tm1 共享图集物理上无窗口私有区 → **v8 动态分配在 tm1 是物理必需**（pokeE 窗口预留模型在 tm1 不可复制）；退役面收敛为：tm3→官方网格公式（chs_tm3_grid_tile）、tm0/tm2 保持官方游标、tm1 保留 v8。
- 字库 2.0 实装：scripts/build_font_1bpp.py 生成 1bpp 位流库（pokeE 11×11/9×9 bin 按 charmap 序重排；GB2312 汉字序已实测验证「一/乙/屹」；6758 直搬+49 自 4bpp 派生+361 空），输出 graphic/fonts/PokeRSFontChs{Big1Bpp_unshadow(0x1C000),Small1Bpp_unshadow(0x13400)}.bin；位流=每行 11/9 位 MSB-first 连续，大库行偏移 1/小库 2。
- hook 新文件 src/text/chinese_glyph.c：chs_cell_from_1bpp 位流→128B 单元（墨 15/右下阴影 14/空 0，TL/BL/TR/BR 布局），纯函数栈上无 EWRAM。
- GetGlyph 三源路由：lib1=1bpp 大库(0x09500000) lib3=1bpp 小库(0x09600000) lib4=旧 Small 4bpp(tm2 专用) lib0=按 fontNum 4bpp 兼容；Middle(lib2)→按 3 处理退役。
- resolve_draw 两档制：tm2→Small4BPP 8/8；fn4/req8→1bpp 小库 adv10/ink9；默认→1bpp 大库 adv12/ink11。场景字号表 scene_cfg 清空（kOptionScene 等全删）。advance/ink 分离（print_glyph_px(win,g128,ink,advance)，adv=(phase+advance)>>3，v8_phase_advance(advance)）——11px 墨 12px 步进的相位循环仍 0/4。
- tm3 下半 tile 偏移 +30（chs_lower_delta），print_glyph_px/chs_fill_bg/chs_place_col 全参数化。
- 流水线：font_patch.py _generate_fonts_s 新增 extra_bins 支持（.org+.incbin，须 _unshadow 命名跳过标点补丁）；config_loader 透传 extra_bins；font.config.json 增 Big1Bpp@0x09500000/Small1Bpp@0x09600000。文件偏移=VMA-0x08000000（0x9500000→0x1500000，勿再算错）。
- 构建：16 obj 零警告（+chinese_glyph.o），game.bin 9400B（+344）；meowth full 1m19s；ROM@0x1500000/0x1600000 逐字节一致；check_rom_hook=True。审计「Small 空槽 2214」=未使用槽（使用槽 0 空），无害。待实机验收：对话框(11×11 新观感)/血条(8px 不变)/tm3 场景/领航员。

## 2026-09-08 中午：1bpp 字形垂直偏移修正（首批实机截图反馈）
- 用户三图（组队选择/宝可梦信息/能力页）反馈：文字整体偏高 ~1px。根因=1bpp 转换行偏移沿用 pokeE 原版语义（大1/小2），我们渲染栈的垂直落位比 FRLG 再高 1px。
- 修正：text_translater.c 新增 CHS_1BPP_ROW_OFF_BIG=2 / SMALL=3（原值 1/2），只动单元内墨迹起始行，水平/相位/游标零改动。要再微调只改这两个宏。
- 构建：text_translater.o 单文件重编 → game.bin 9400B（尺寸不变）→ syms 5 符号地址不变 → meowth full 1m19s → check_rom_hook=True。待用户实机复验。

## 2026-09-08 下午：SMALL 旋钮「无效」根因（路由分治）+ 4bpp Small 行偏移旋钮
- 用户反馈：CHS_1BPP_ROW_OFF_SMALL 改 10 无效、BIG 正常。根因=路由：血条名等可见小字走 tm2→CHS_FONT_LIB_SMALL4BPP（旧 4bpp 128B 库，垂直位置烙在 bin 里）；1bpp 9×9 小库只有 fn4/req8 窗口走，当前场景零使用 → 旋钮无观察面。另：SMALL=10 时若真走 1bpp 路径，9 行墨只剩 6 行（line_off+r<16 裁剪），已复位 3。
- 新增 CHS_SMALL4BPP_ROW_OFF（默认 1）：GetGlyph 4bpp 分支 fontNum==4 时 copy_tile32_shift_down 整单元下移（顶部补空行，4 tile 各 8 行×4B）；0=维持打包原位。game.bin 9520B（+120）。
- ⚠ Bash CWD 本轮多次漂移（cd 不持久，根目录残留旧架构 out/obj：text_render_band.o 等 9 个旧 obj 勿混淆）；构建命令必须 cd+验证文件存在性同链。
- 旋钮速查：对话框/summary 大字=CHS_1BPP_ROW_OFF_BIG；fn4 9×9=CHS_1BPP_ROW_OFF_SMALL；血条名 8px=CHS_SMALL4BPP_ROW_OFF。均在 text_translater.c 头部。

## 2026-09-08 下午：两档制 2.0——旧 8px→9×9、旧 12/16px→11×11（用户拍板，旧 4bpp 渲染全退役）
- 用户指令：旧的8px替换为9px，旧的12、16px替换为11px。落地：resolve_draw tm2 分支改 lib=1BPP_SMALL（adv10/ink9，与 fn4/req8 同档）；旧 Small 4bpp（lib4）路由删除，CHS_FONT_LIB_SMALL4BPP 常量、CHS_SMALL4BPP_ROW_OFF 旋钮、copy_tile32_shift_down 助手全删（grep 验证零残留）。lib0（4bpp 兼容）保留编译但无路由。12/16px 在地基更换时已无路由（默认 11×11），本次确认。
- 新增 tm2_print_px（PrintNextChar_hook.c）：血条 win[0x20] 线性缓冲的相位两段式发射（列槽=0x40 上下半两 tile，dst+=adv*0x40，尾列 w1..8 清底+w1==0 时本列补清，防残留）；相位用全局 v8_phase（血条名是独立会话，InitTextPrinter 复位覆盖）。chs_emit 删 tm2 强制 advance=8，tm2 直接路由 tm2_print_px。chs_rasterize 8/16 分支保留编译但已死代码。
- ⚠ 风险（待实机验收）：tm2 每字列耗从 1 列（8px）变 1~2 列（10px 步进，4 字 5 列），长名（≥5 字中文或混符号）可能溢出血条名槽；若溢出需回 8px 或压步进。
- 构建：16 obj 零警告，game.bin 9696B（+176）；syms 地址不变；meowth full 1m16s；check_rom_hook=True。旋钮：CHS_1BPP_ROW_OFF_BIG/SMALL 管 11×11/9×9 垂直偏移（text_translater.c 头部）。
## 2026-09-08 hook build.bat 修复 + 双源树确认
- 用户报 hook build.bat 链接错误 chs_cell_from_1bpp 未定义：根因=build.bat 硬编码清单漏了 chinese_glyph.c。已补编译行+链接行，bash 复刻 16 obj 构建验证通过（game.bin 9696B）。
- 用户拍板：**以 config 树（configs/POKEMON_RUBY_AXVJ00/hook）为准**，CHS_1BPP_ROW_OFF_SMALL=4 是用户故意调的（实验 SMALL=4 效果）；work/POKEMON_RUBY_AXVJ00/build 树为旧副本（SMALL=3）。
- 推论：check_rom_hook 对比 work 树产物会误报 False；后续校验应以 config 树构建产物为基准。
- 教训：缺 obj 清单这类单点修复，改完 build.bat 即止，不要顺带跑 check/对比流程扩散排查。
- 用户明确表态：check_rom_hook.py 视为遗留垃圾、压根不会用；自造校验脚本反被批评"自己写垃圾留坑"。教训：校验手段先问用户要不要，别主动造工具；未验收的东西不主动挂到流程里。
## 2026-09-08 hook 死代码清理（用户指令：删掉不用的方法/hook）
- 删除（全部零调用者/两档制后不可达，grep 全树验证）：
  ① PrintNextChar_hook.c：v6_scene_lookup/zone/font（场景表访问器）、chs_rasterize+chs_place_col+chs_emit 16px 回退段（advance 恒 12/10/8）、#if CHS_ADVANCE_12 包装
  ② blend_glyph.c/h：blend_glyph_1bpp/2bpp+blend_row_1bpp/2bpp（1bpp 字形转 g128 后恒走 4bpp blend）
  ③ text_translater.c：GetGlyph lib0 4bpp 兼容块（改为 return 0）、slot_lookup_legacy+fnv1a_hash（SLT2 magic 校验保留，失配=放弃）
  ④ tile_alloc.c/h：v8_phase_newline_reset（FE 拦截已删，零调用者）
  ⑤ 整文件删除：scene_cfg.c/scene_cfg.h/scene.h(v4 孤儿)/render.h(孤儿)/scripts/check_rom_hook.py(用户明确不用)
  ⑥ game.h：CHS_ADVANCE_12 宏删，CHS_GLYPH_ADVANCE_PX=12 固定
- build.bat 同步：删 scene_cfg 编译+链接行（清单变 15 obj）。game_syms.asm 必须用新 map 重生成（布局前移，符号地址全变）。
- 验证：15 obj 编译零警告，链接成功，game.bin 9696→8192B（-1504B 死代码）。
- 【待实机确认】渲染语义零变化预期：所有删除不可达，画面应与删前完全一致；SMALL=4 旋钮不变。
