# 2026-09-23 工作日志

## text 重构总体方案产出（用户任务：熟悉工程 + 全网调研 + 出方案）

- 全文清理瘦身 `.workbuddy/memory/MEMORY.md`（超限截断后重写，保留铁律/死路/速查/铁律四块）。
- 通读工程文本链：`AXVJ_TEXT_PIPELINE.md`、`REWRITE_DESIGN_混合写入架构.md`、0923 三份调研/方案、
  `重构_20260908_pokeRS式地基更换蓝图.md`、`评估_20260829`；实读 hook/src/text 全部模块
  （PrintNextChar_hook / tile_alloc / InitTextPrinter_hook / opt_pixel / win_canvas / softbuf 头部）。
  确认现状基线 = v8/v9 + S1，win_canvas/softbuf 已退出链接。
- 全网新检索（6 组）：GBA 汉化教程链（码表/字库/指针）、rh-reborn fonts 指南（VWF/skinny/dual-tile）、
  Mother3 Text Welding 102（VWF 移位+OR 合并标准）、KaioShin DQM VWF 源码（romhacking.net/docs/337）、
  gb-vwf/gb-photo vwf.c、pret pokeruby text.c 抓取复核（确认美版无像素缓冲、sGlyphMasks 亚 tile 写入、
  mode2 返 start+2+w*h、DrawDownArrow (x&7) 分割）。
- 产出 **`docs/方案_20260923_Ruby_text重构_总体方案.md`**：现状盘点 + 全网调研汇总
  （含非 8/16px 渲染五类技术表 + 数学极限）+ 根因 + 分期路线
  R0 清障 → R1 渲染层零状态化（A:单次两段直算 / B:8×12 窄字形）→ R2 tm1 勘测（金标）
  → R3 所有权落地（C2 声明式段 / C1 窗生命周期 / B 美版契约）→ R4 收敛。
  未改任何代码；4 个拍板点留给用户。

## tm1 对齐美版 / 寻址统一 / 12px 容量分析（下午追加）

- 用户新问四题（能否反汇编把 tm1 对齐美版 / 与其他 tm 统一 / 本质是否无区别 / 12px 是否注定 cb 不够）。
- 新增静态普查 **`src/util/work/POKEMON_RUBY_AXVJ00/_tm1_census_20260923.py`**（只读 ROM）：
  **52 个窗模板 / 36 个 tm1**（cb0 14、cb2 14、cb3 2、cb1 1）；font3 图集 512 tile 占满整 cb、
  框带靠 SetBase 翻 charBase 落下一 cb；连号同屏家族明显（真实窗家族约 8~12）；
  11 个已登记模板地址全部对上（字段启发式 0x18 步进连续表）。
- 产出 **`docs/分析_20260923_tm1对齐美版_寻址统一与12px容量.md`**：
  - tm1 ≡ 美 mode1（同为内容寻址图集）；tm0/tm2 线性、tm3 位置网格；所有权四者同构
    （开窗认领+顺序接框）；统一终态 = 全位置寻址 ⇒ v8 可退役。
  - 12px 画布模型行耗 = 行像素/8 = 27 tile/行（18 字），比 16px 整格（28）还省；
    「费 cb」是每字领号模型的放大效应（v8 36~72 tile/行 + 无跨重绘复用）。
  - 容量【推论】：设置页 30×16 画布 482≤510 可容于 cb2；全屏 30×20=602 需 cb2+cb3
    1024 跨段，是否需 BG 重排由按屏共存表（R2 gdb）定。
  - R3 修正：**C2 声明式空闲段升级为默认主路线**；C1（改 TLWT 返回值）在 S2(e6f0 全 xref)
    完成前冻结；B 按窗家族迁移，配套静态清单 S1~S7（TLWT xref/Continue+e6f0 xref/SetBase/
    Multistep 轮询/WriteGlyphTilemap 家族/ClearWindow/7 特例表）+ gdb G1~G3。
- 未改任何 hook 代码；未启动模拟器。

## C2 声明式段勘误（傍晚，用户质疑触发）

- 用户指出「声明式画布段以前做过，效果特别差连基础字都不显示」→ 考古证实**记忆正确**：
  v4/v4.1 全屏"1"（N1 算术误读）、v5–v8 安全块 [121,322] 踩字形（B1 引用稀疏≠占用稀疏）、
  v6/v7 静态选址「早已废弃」（scene_cfg.h:5 自证）、08-31 kTm1Windows 例外条款**从未实施**（grep 无代码）。
- 0923 错误定位：把评估 N4' 当 C2 先例引用——前提恰被 B1 证伪。
- 产出 **`docs/勘误_20260923_C2声明段_历史对照与裁定.md`**；三份 0923 文档 C2 判定收回
  （分析 §5 / 方案 R3 / 方案 v2 R3；v2 首次写入遇 EBUSY 重试成功）；
  主路线回归 **B 四件套**（与 09-09 裁定一致），C1 冻结，S1~S7/G1~G3 勘测清单保留（服务 B）。
- 公平记录：图集已停用 + 现行 v9 三闸门实机可用 ⇒ 死因是「静态知识断言占用」而非区域本身；
  全网成活引擎无一用静态段（全为开窗运行时分配+所有权+生命周期，与 B 同构）。

## S1~S7 离线静态反汇编勘测（傍晚执行，用户拍板）

- 用户拍板「先跑 S1~S7」→ 新建 **`_s1_s7_survey_20260923.py`** + **`_s1_s7_addendum_20260923.py`**
  （capstone 全 ROM BL/字面量扫描 + 定向反汇编，只读 ROM 未开模拟器；输出 `_*_out*.txt`）。
- 关键定案（详见 **`docs/勘测_20260923_S1-S7_静态反汇编.md`**）：
  - S1：TLWT 0x08002950 仅 2 个 BL（0x080685CA 任务流 / 0x0806F006 泵态）、0 字面量；
  - S2 勘误：0x0806F00A 非函数入口，是泵状态机内 TLWT 后落点；e6f0 记录消费 51 字面量集中在 0x0806E8xx~0x0806F4xx（离群 5 处）；
  - S3：SetBase=14 直调+13 中段（0x08062094）=27 处；
  - S4：Multistep=每调 16 字形、满 256 返 1；stub(mov r0,#1) 静态等价成立；
  - S5：WriteGlyphTilemap 三函数零直调，唯一入口=FontFuncTable[4..6]/FontSubTable ⇒ 改 4 表项全覆盖；
  - S6：日版 Text_ClearWindow=0x08003BA8（4 BL：0x08002F56/08002F8E/080032EA/0806F3D2）+ 行清 0x08003C00（4×lineLength u16、步进 32；mode0 清后 win[0x18]=4 美版为 2）；
  - S7：美版 58 项全解析、50 标准+7 特例与 0910 文档一致（首轮 2/52 是校验器误杀 EWRAM 缓冲窗）；美 tm2=EWRAM 像素缓冲（td=0x02039360,tmap=0）⇒「美 mode2 画布」术语修正，tm1 迁移对照 mode0 画布；日 tm1 归 15 家族。
- 分析_20260923 §4 标注 S1~S7 已关闭+两勘误；MEMORY.md 同步（2996 字符）。
- 遗留 gdb 半区 G1~G3：模板→流绑定 / 按屏共存表 / e6f0 当帧时序。未启动模拟器，未改 hook 代码。

## G1~G3 运行时化裁定（晚，用户问「是否必须逐窗采集」）

- 用户质疑「还得一个个窗口采集？无法深入底层改？」→ 澄清定案并写入勘测文档：
  - **修改面就是底层一小撮**（4 表项+2 开窗点+ClearWindow×2+泵缝 1 区+停图集 stub），36 tm1 窗经表分派一次全切，**无逐窗改代码**；
  - G3（模板→流绑定）**降级为不需要**（表级替换下无论哪条流进来行为统一）；
  - G1（按屏共存）改为**运行时自预算**（开窗当场分配+v9 式当帧校验+超额显性失败；美版自身也不预量）；残余物理未知仅 Arena 回收安全（S3 的 27 处 SetBase 清单静态论证+实机抽查）；
  - G2（e6f0 时序）缩为**实施后一次采样**（B 不动返回值则 seam 不受影响）。
- 净效果：gdb 半区从「15 家族逐窗」缩为「最坏屏抽查 3~5 + e6f0 一次采样 + Arena 论证」，随实施批次做，不阻塞开工。

## B 底层切换实施单产出（晚，用户拍板「继续」）

- 实读装配层定案：**现行 v6 唯一文本 hook = PrintNextChar@0x080032F8 + InitTextPrinter@0x08002C68 桩，
  FontFuncTable 不重定向**（hooks_origin.s 头注释 + main.asm 自证）——日版原生表路径未动，中文全在泵层拦截。
- 产出 **`docs/实施_20260923_B底层切换_实施单.md`**：
  - 切换面 C-1~C-8（泵/开窗/清窗/e6f0/SetBase/blend_glyph 复用/fn4 不碰）；改动面收敛为 C-1~C-5；
  - 闸门从「每字形」收缩为「开窗认领+关窗释放」两事件，认领失败=警戒色显性失败；
  - Arena 论证：图集停用后 4 cb 即 Arena，v8 时代中文已实机写通=既成事实，非文本装载风险交给当帧校验兜底；
  - T0 tag → T1 开窗认领 → T2 位置直写+删相位 → T3 清窗释放 → T4 v8 退役 → T5 gdb 采样，每步独立回退；
  - 最坏屏验收 6 项 + 禁区逐条对照。
- 未改任何 hook 代码；T0 起实施等用户指令。

## T0+T1 实施完成（傍晚，用户「开工」）

- **T0 基线固化**：备份 hook src/include 至 `_backup/T1_pre_20260923/`（含 game.bin.baseline）；
  git tag `baseline-pre-B-switch-20260923`；基线构建绿（8988B）。
  工具链定位：arm-none-eabi 在 `C:/Program Files (x86)/Arm GNU Toolchain arm-none-eabi/14.2 rel1/bin`（不在 PATH，构建时手动加）。
- **T1 画布认领落地**（改动面 7 文件）：
  - 新增 `hook/include/canvas.h` + `hook/src/text/canvas.c`：
    EWRAM 记录块 0x0203FF5C..FF76（magic@FF5C + rec0/rec1 各 12B + flag@FF76；
    rec={win,tpl,base,n}，该区 grep 证无占用，上邻 v9 键区/下邻游戏数据区均不碰）；
    认领=开窗边界 v8_ui_alloc 一次领断 2+w×h（默认 30×20，特例覆盖表 kCvDims 现为占位）；
    同 (win,tpl) 续接不重领；失败置 flag bit0 显性回落 v8 路径（零视觉变化）。
  - `tile_alloc.c`：v8_alloc_begin 尾部 canvas_protect(bm)（重建后重标画布区间）；
    `InitTextPrinter_hook.c`：begin 后调 canvas_on_begin；`game.h` 加 4 个 ADDR；
    `build.bat`/`build_sh_equiv.sh` 加 canvas.o（铁律同改）。
  - 已知边界（T2 前必须补，已写入 canvas.c 头注释）：记录淘汰后空行可被扫走，T2 写字形前须校验行 tile 仍在本窗记录内；cb3 窗（2 模板）602>256 必失败→回落，特例实机定位后填表。
- **验证**：构建绿（10080B，+1092）；armips 装配绿（⚠路径=../../../tools/armips.exe，AGENTS.md 的三层回退正确、我首跑写错两级）；game.bin @0x08800000 逐字节嵌入 ✓。
  ⚠ AGENTS.md 提到的 scripts/check_rom_hook.py 已不存在（文档过时），嵌入校验用内联脚本完成。
  output.gba=汇编验证流程产物（origin+hooks+fonts+slot），非 full 链验收 ROM。
- 未启动模拟器；实机验收点：对话/菜单/设置页显示与今日一致 + gdb 观察 ADDR_CANVAS_FLAG=0（或记录回落场景）。

## T2 落地（17:00~18:00 前后，画布位置直写）
- T1 用户实机验收「正常」→ 已 commit 45b8445（含 0923 全部文档与记忆）。
- **C-1 渲染**：PrintNextChar_hook.c 新增 tm1_canvas_print——tile=base+2+row×w+col
  （row=win[0x1D] cursorTileY、col=px>>3），下半 tile=+w（画布行距，不再 +1）；
  无领号/无 v8_phase_last_tile 尾列推导（相位三件套在画布路径不存在）；
  相邻字共享 tile 列由 blend_glyph RMW 天然承接；表项仍走原生 UpdateTilemap
  （cell 列−窗左缘 ≡ 画布列，两序列同步）；越界（col/row 出画布）跳过本字宁缺不砸。
- **C-4 最小形态自 T3 提前**（推演发现页清必须同步清像素，否则第二页 RMW 叠第一页残墨）：
  Text_ClearWindow@0x08003BA8 挂 8B 桩（ldr r1/bx/pool，盖 BA8..BAF）；
  entry.s TextClearWindow_Hook：push{r4,lr}重放→保参→bl TextClearWindow_hook_C
  （tm1+lookup 命中→canvas_fill 整块填 color_d）→重放 adds/ldrb/cmp→bx 0x08003BB1。
  反汇编钉死原序言 4 半字（push/adds/ldrb/cmp）、全 ROM 无跳转目标落入 BA9..BB1、
  4 个 BL 调用方字节未动、BB0 起未触碰。game_syms 清单 build.bat+sh 同步加 TextClearWindow_Hook。
- canvas.c：记录 +10/+11 改存 w/h（n=2+w×h 可推导）；认领成功即 canvas_fill 首屏清底；
  新增 canvas_lookup（键 win+tpl，返回数据基址 base+2 与 w/h）/canvas_fill/TextClearWindow_hook_C。
- C-5 e6f0：hook 源码零引用（记录由 TLWT 写、消费面图集语义已死）→ 不改码，T5 gdb 采样核对。
- 验证：构建 11264B（+1184）零告警；armips 绿；game.bin@0x08800000 逐字节嵌入✓；
  钩桩反汇编逐条核对✓。未跑 full 链（等用户实机验收指令）。
- 验收点：长对话连翻 5 页（页清/无残墨）、菜单遍历、战斗叠加、设置页、fn4 队伍窗。

## T3+T4 落地（19:00 前后，关窗释放+退役裁定，待实机）
- T2 用户实机验收「正常」→ 已 commit 328e3cd（out 目录用 -f；嵌套 .gitignore hook/.gitignore 有 out/ 规则）。
- **T3 关窗释放**（canvas.c，无新 ROM hook）：①陈旧记录主动释放（owner 同 win 异 tpl ⇒ 必死，
  canvas_on_begin 入口、tm 过滤前）；②淘汰智能化：陈旧>rec0，不再盲摘 rec0（修复第三窗打断对话）；
  ③回落显性计数 canvas_note_fallback → 0x0203FF77（饱和255），chs_emit tm1 回落前调用。
- **释放语义勘误**：ours 段表不删（=可回收白名单，删=泄漏）；只清记录槽；位图不显式清（下次 begin 重建）。
- **T4 裁定**：主路径逐字领号 T2 已退役；v8_alloc_n/last_tile 保留为回落引擎（cb3 602>256 依赖），
  删除门槛=回落计数整轮恒 0（kCvDims 覆盖全部回落窗）；phase_* 现存=px 游标=渲染根本状态，不在退役对象。
- game.h：+ADDR_CANVAS_FB_CNT 0x0203FF77；画布块注释更新（FF78..FF7F 空闲）。
- 验证：构建 12004B(+740) 零告警；armips 绿；嵌入逐字节✓；桩/续跑区逐位未变✓；无新 ROM hook。
- 验收点：对话中叠开两菜单再回对话；长会话后 gdb 读 0x0203FF77（>0 场景名→填 kCvDims）。
