/* Obsolete: style left lives in styles_data.asm @ ADDR_STYLE_* (see game.h).
 * Kept so stale includes do not break; do not put build-time px here. */
#ifndef LAYOUT_LEFT_H
#define LAYOUT_LEFT_H
#endif
