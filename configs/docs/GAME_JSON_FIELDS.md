# game.json 字段说明

> 仅身份字段。阶段配置见 [`PROFILE_FIELDS.md`](PROFILE_FIELDS.md)。

| 字段 | 用途 |
|------|------|
| `game_id` | 配置目录与检测 ID |
| `label` | GUI 显示名 |

其余（tables / extraction / font_patch / line_width / charmap…）已迁入：

- `modules/`、`extract/`、`tables/`、`translate/`、`font/`、`patch/`
