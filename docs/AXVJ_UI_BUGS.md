# AXVJ UI / 文本 BUG 台账（逐项修）

原则：**一次只修一类**；出 ROM → 你测 → 说「正常」后本地 commit → 再开下一项。

## 当前进度（2026-08-09）

| ID | 现象 | 状态 |
|----|------|------|
| B01 | 存档信息框花屏 | **已验收**（PrintSave 扩栈+拷 16B） |
| B02 | 战斗台词截断 + 放技能狂打死机 | **PrintNextChar 层方案全部失败**；已回 B01 |
| B03 | 地名弹窗顶框 | B01 基线正常；凡动短语打印必回归检查 |
| B04 | 商店/背包光标 | 未修 |
| B05 | 血条昵称 | 未修 |

## B02 — 战斗（重新定性 + 失败账）

### 现象（你的纠正）

- 遇敌等：不是「多首字」，而是后缀被截断（「野生的××出来了」后半没有）。
- 放技能：无限出字卡死。

### 曾判机制

中串 `F9 op hi lo` → `redirect` 到 PhraseTable → 短语 `FF` 被当成整窗 EOS。

### 已在 PrintNextChar 试过且失败（禁止再走）

| 代号 | 做法 | 结果 |
|------|------|------|
| B02a | ROM `.bss` / IWRAM PhraseResume + 切流 | 地名炸；战斗仍坏 |
| B02b | 父串续打才整句 Draw 内联 | 地名微顶；战斗仍坏 |
| B02c | 展平到 EWRAM 再逐字打 | **地名严重顶出；战斗无变化** |

结论：**再改 `print_next_char` 的 F9 短语分发，性价比极低**（易伤地名、战斗侧未见效）。  
下一刀应离开该文件：例如战斗串 WRAM 构建点展平、或物种/招式注入策略（需新证据），开干前先书面方案 + 你点头。

### 代码状态

工作区 **仅保留 B01**（`hook_origin.s` / `game_addrs` PrintSave*）。  
`print_next_char.c` 短语路径 = 原版 `redirect_phrase_stream`（与验收 B01 时一致）。

## B01 — 存档 — 已验收

`PrintSavePlayerName/PokedexCount/PlayTime`：`sub/add sp,16` + `mov r2,16`。

## B03 — 地名

以 B01 基线 ROM 为准。任何 B02 试验后先看地名是否回退。

## 约定

你说「正常」→ 本地 `git commit` 该项。
