# GBA Rom Translator 技术文档

## 一、GBA 硬件 Tile 基础

GBA PPU 的 BG 图层基于 **8×8 像素 tile**，每 tile 可选 4bpp（16 色，32 字节）或 8bpp（256 色，64 字节）。本工程使用 4bpp 模式。

tilemap 是一维 u16 数组，每个条目引用 VRAM 中的一个 tile 索引。BG 扫描时按行读取 tilemap，将对应 tile 渲染到屏幕。

**显存访问限制**：VRAM 禁止 byte 写入（GBA 硬件会把 byte 镜像到半字的另一字节导致鬼影），所有像素合成必须在 IWRAM 完成，最后以 32-bit word 批量拷贝回 VRAM。

---

## 二、16×16 字模 + 12px Advance

### 问题

中文汉字通常是 16×16 点阵。GBA 硬件以 8×8 tile 为单位，16×16 字符自然地映射为 4 个 tile（2×2 排列）：

```
+----+----+
| TL | TR |   ← 行 0-7
| BL | BR |   ← 行 8-15
+----+----+
```

但中文排版期望 12px 字间距（更紧凑、可读性更好），而非 16px。如何在 tile-aligned 的硬件上实现 12px advance？

### 方案

字模存储保持 **16×16 标准 4-tile 布局**（128 字节/字，`ADDR_FONT_CHS_NORMAL @ 0x09000000`），渲染时光标只推进 12px，利用 **spill 重叠** 实现紧凑排版。

### 实现细节（`draw_glyph.c:drawGlyph12`）

每字分两趟写入 VRAM：

| 趟 | 写入宽度 | 来源 | 光标推进 |
|---|---|---|---|
| 1 | 8px | 字模左半 TL+BL（`src128[0x00..0x3F]`） | +8px |
| 2 | 4px | 字模右半 TR+BR 的左边 4px（`src128[0x40..0x7F]`） | +4px |
| **合计** | **12px** | | **12px** |

`startPixel = st->chs_px & 7` 决定每趟在当前 tile 列内的起始像素偏移。`chs_px` 每趟累加，跨字后按 12px 步进，奇数个字 `startPixel=4`，偶数个字 `startPixel=0`。

```
chs_px=0  → startPixel=0  写法：左8px占tile列0 px0-7，右4px跨入tile列1 px0-3
chs_px=12 → startPixel=4  写法：左8px占tile列1 px4-11，右4px跨入tile列2 px0-3
chs_px=24 → startPixel=0  循环...
```

### spill 处理（`draw_glyph.c:draw_glyph_tile_12`）

当 `startPixel + width > 8` 时触发 spill，同时处理当前 tile 和下一 tile：

1. 从 VRAM 读取当前 tile + spill tile 到 IWRAM（`dest_l` / `spill_l`）
2. 清掉本字占用的像素列（置为背景色 `color_d`）
3. OR 上墨水像素（ink color `color_c`）
4. 8 次 32-bit word copy 写回 VRAM

下一字左 4px 覆盖上一字右 4px。由于汉字笔画集中在字模中部，外缘空白区域被覆盖不影响视觉。

### 宽度计算（`get_string_width.c`）

`GetStringWidthChinese_Full` 遍历字符串：
- `F9` 序列（单字/短语）：累加 `CHS_GLYPH_ADVANCE_PX = 12`
- JP 半角字符：累加 8px
- 控制字节 `0xFA..0xFE`：跳过

与 `drawGlyph12` 保持一致的 advance 步进，确保排版对齐。

---

## 三、短语表（PhraseTable）

### 问题背景

原版 Gen3 的固定长度字段有 stride 限制：

| 模块 | 日文 stride | 直接侧载上限 |
|------|------------|------------|
| 物种名 | 6 字节 | `F9 00 × 1` = 1 汉字 |
| 招式名 | 8 字节 | `F9 00 × 2` = 2 汉字 |
| 特性名 | 8 字节 | `F9 00 × 2` = 2 汉字 |
| 属性名 | 5 字节 | `F9 00 × 1` = 1 汉字 |

`F9 00 ll tt` 每汉字 4 字节，在 6-8 字节的槽里只能塞 1-2 个汉字，无法覆盖中文翻译需求。

### 方案：存储-引用解耦

短语表将文本存储与字段引用分离：

```
字段槽（固定长度）          ROM 扩展区（无限制）
┌────────────────┐         ┌─────────────────────────┐
│F9 <op> hi lo FF│  ──→   │PhraseTable[code]:        │
└────────────────┘         │  count=4                │
  ↑ 4 字节引用            │  idx[0]=0x1234          │
                           │  idx[1]=0x5678          │
                           │  idx[2]=0x9ABC          │
                           │  idx[3]=0xDEF0          │
                           └─────────────────────────┘
```

字段槽只放 `F9 <op> <code_hi> <code_lo> FF`（4 字节），无论译多长槽大小不变。

### 内存布局

```
地址         内容                    生成方
0x08800000   game.bin（C 代码）      arm-none-eabi-gcc 编译
0x08810000   PhraseOffsets（u16[]）  phrase_data.asm（Python 生成）
0x08820000   PhraseTable（条目）     phrase_data.asm（Python 生成）
0x09000000   字库 Normal（7168 字）  BDF 栅格化 → .bin
0x09100000   字库 Small（7168 字）   BDF 栅格化 → .bin
```

- `PhraseOffsets`：u16 数组，`offsets[code]` = 条目在 PhraseTable 中的字节偏移
- `PhraseTable` 条目格式：`{u8 count, u8 pad, u16 glyph_idx[count]}`

### 构建流程（Python）

```
1. translate/lexicon/*.json  →  load_custom_translations()
   → dict {日文原文: 中文译文}

2. engine._build_rom()
   ├─ 收集译文（>1 汉字）→ 分配 code 0..N
   ├─ charmap.encode("大招") → F9 7F 00 03 FF
   └─ rom_writer 将 F9 7F 改写为模块对应的 op（如 F9 04）

3. engine._ensure_default_fonts()
   ├─ 中文 → 字库索引映射（charmap._lead_adjust）
   ├─ 生成 phrase_data.asm：
   │   .org 0x08810000
   │   PhraseOffsets: .halfword <偏移列表>
   │   .org 0x08820000
   │   PhraseTable:
   │     .byte <count>, 0
   │     .halfword <glyph_idx_1>, <glyph_idx_2>, ...
   └─ 最大 8 汉字/短语（MAX_PHRASE_CHARS = 8）
```

### Runtime 查找路径

```
PrintNextChar_C:
  └─ cur_char == 0xF9
       ├─ op == 0x00  → pack_glyph_index → DrawGlyph_Chinese（单字侧载）
       └─ op ∈ [0x01..0x7F]  → draw_phrase(code)
              ├─ PhraseOffsets[code] → table_offset
              ├─ PhraseTable + table_offset → {count, idx[]}
              └─ DrawGlyph_Chinese × count（每个走 12px advance）
```

### F9 op 字节的复用

`op` 字节同时承载渲染模式选择：

| op | 语义 | write_op | 渲染模式 |
|----|------|----------|---------|
| `0x00` | 单字侧载 | 无 | Linear / Mode2 自动 |
| `0x01` | 物种短语 | `CHS_WRITE_GRID=1` | Mode2 grid |
| `0x02` | 队伍底栏 | `CHS_WRITE_FOOTER=2` | Mode2 footer |
| `0x03` | 线性短语 | `CHS_WRITE_LINEAR=3` | Linear |
| `0x04` | 招式/道具 | `CHS_WRITE_SLOT=4` | Slot |
| `0x05` | 属性短语 | `CHS_WRITE_AUTO=0` | 自动 |
| `0x7F` | 通用短语 | `0`（重置） | 自动 |

`DrawGlyph_ShouldUseLinear` 根据 `write_op` + 窗口模板（`charBase`）判断用线性 tile 推进还是 Mode2 菜单/队伍池寻址。

---

## 四、模块注入（Module Inject）

### 固定长度表（fixed_table）

日版文本以定长记录存储在 ROM 中。注入时改写 stride 以容纳中文：

| 模块 | 解析 stride | 写入 stride | 补丁 | 说明 |
|------|------------|------------|------|------|
| 物种名 | 6 字节 | 24 字节 | `literal_ref_widen` (mul6→mul24) | 所有指针目标同步加宽 |
| 招式名 | 8 字节 | 16 字节 | `literal_ref_widen` (mul8→mul16) | `write.type=op`, op=0x04 |
| 特性名 | 8 字节 | 16 字节 | `literal_ref_widen` (mul8→mul16) | |
| 道具名 | 40B entry, name@8 | 16 字节 | `item` | |
| 属性名 | 5 字节 | 5 字节（原地） | 无 | 最短、无需加宽 |
| 性格名 | 指针表 | 同 | 无 | 间接引用 |

### 文本换行宽度

| 模块 | `line_width` |
|------|-------------|
| 道具说明 | 13 |
| 开始菜单 | 5 |
| 队伍选项 | 8 |
| 默认 | 20 |

### 指针重写（literal_ref_widen）

加宽表后，所有指向旧 stride 的编译时常量指针必须同步更新。`literal_ref_widen` 在 ROM 中扫描匹配模式（ARM/Thumb 字面量池），将 `base + i * old_stride` 改写为 `base + i * new_stride`。

---

## 五、构建流水线

```
meowth full <rom> --seed-only --target zh-Hans

1. extract（提取日文原文）
   ├─ 扫描 script text（筛选 ptr + reject addr_bands）
   ├─ 提取 fixed_table / struct_table 模块文本
   └─ 输出 translate/modules/*.json

2. seed-translate（离线翻译）
   ├─ 加载 lexicon/*.json 种子
   └─ 填充目标语言字段

3. font（字库生成）
   ├─ BDF 栅格化 → work/graphic/fonts/*.bin
   └─ 生成 charmap.txt

4. build（补丁 + 注入）
   ├─ make / gcc 编译 C 源码 → game.bin + game_syms.asm
   ├─ 生成 phrase_data.asm（PhraseOffsets + PhraseTable）
   ├─ armips main.asm：
   │   ├─ hook ProcessCurrentChar → PrintNextChar
   │   ├─ hook GetStringWidth → GetStringWidthChinese
   │   ├─ .incbin game.bin @ 0x08800000
   │   ├─ .org phrase_data（Offsets @ 0x08810000, Table @ 0x08820000）
   │   └─ .incbin 字库 .bin @ 0x09000000 / 0x09100000
   └─ 注入翻译文本（改写日文字段 + 短语引用）

5. output（输出 GBA ROM）
```

---

## 六、关键约束汇总

| 约束 | 值 | 定义位置 |
|------|-----|---------|
| game.bin 最大体积 | 0x10000（64KB） | `font_patch.py:24`, `game.ld:4` |
| PhraseOffsets 地址 | 0x08810000 | `game.h:17` |
| PhraseTable 地址 | 0x08820000 | `game.h:18` |
| 每短语最大字数 | 8 | `engine.py:1213` |
| 字库 Normal 容量 | 7168 字 × 128B | `font/config.json` |
| 每字 advance | 12px（大字），10px（小字） | `game.h:61` |
| 每字存储大小 | 128 字节 = 4 × 8×8 4bpp tile | `game.h:64` |
| F9 逃逸字节 | 0xF9 | `game.h:100` |
| F9 通用短语 | 0x7F | `game.h:101` |
| F9 单字侧载 | 0x00 | `config_loader.py:55` |
| F9 op 范围 | 0x01..0x7E | `config_loader.py:57` |

---

## 七、绘制通道与已知问题

### 7.1 汉字替换（已解）

详情页昵称 / 亲代 / 数字与中文标签曾走双通道：F9 中文走 CHS 落点，日文 PCS 走原版 `FontFuncTable`，同一 VRAM 池互相覆盖 → 屏上汉字被换成别的字（「汉字替换」）。

**现行策略（禁止回落原版可印路径）：**

| 项 | 约定 |
|----|------|
| 可印 PCS `1..0xF6` | 一律 `GetGlyphTilePointers` 取模 → `DrawGlyph_Chinese_Adv(..., 8)` |
| AXVJ ABI | `GetGlyphTilePointers(font, glyph, &upper, &lower)` **4 参**（日版无 language；美版 pokeruby 多一参，勿照抄） |
| font 3/4/5 | 32B 阴影字模（nibble 0/E/F）直接进 TL/BL |
| font 0/1/2/6 | 8B 1bpp → 扩成 0xF 墨水再走 CopyGlyph2bpp |
| 取模失败 | `return 0` 交回原版；禁止空画却 `return 1` |
| tile 带 | **不**靠再调 Mode2 band 修替换 |

实现：`configs/POKEMON_RUBY_AXVJ00/patch/src/text/PrintNextChar/print_next_char.c`、`game.h`。  
布局细节见 `configs/docs/CHS_TILE_LAYOUT.md`。

### 7.2 Phrase 去重陷阱

构建时 PhraseTable 码位按**中文译文**去重（`engine.py` 对 `_custom_translations` 的 ZH 集合分配 `0..N`）。

后果：不同日文原文若译成同一中文，会共用同一 `F9 7F hi lo`。  
典型事故：能力页空槽 `なし` 误译「取消」，与 `やめる`→「取消」撞同一 phrase → 携带物/奖章两格都显示「取消」。

**处置**：纠错译（如 `なし`→`无`），不要为排版改短词（见 `.cursor/rules/axvj-no-phrase-for-layout.mdc`）。

### 7.2.1 Sym 标点格式（勿当 16×16 2bpp）

`ADDR_FONT_CHS_SYM`（9×64B）：与 Font3 Latin 相同，**上 32B + 下 32B** 的 8×8 4bpp 索引（nibble 0/E/F）。  
误按 16×16 packed 2bpp 解包会把「？」纵向拉成细高椭圆。正确：拷到 CHS TL/BL，`DrawGlyph_Chinese_Adv(..., 8)`。

### 7.3 能力页中文显示（截图清单 → 排查结论）

| 现象 | 类别 | 结论 |
|------|------|------|
| 携带物/奖章下两个「取消」 | 错译 + Phrase 碰撞 | `texts_translated`：`なし@0x083E9AEC` 误为「取消」（同串另一址 `0x083E9963` 已是「无」）。指针表 `0x37B334+0x08/+0x38` 均指向 `F9 7F 00 4A`（与 `やめる` 同 phrase）。**lexicon** 补 `"なし": "无"`（inject 精确匹配强制覆盖缓存） |
| 右上「切换」叠红影 | 次生绘制残留 | `きりかえ`→切换 正确；同页两格错画「取消」进共享池叠在切换上。修 `なし` 后应减轻 |
| 「当前经验值」叠数字发虚 | F9(12px) + JP-via-CHS(8px) 同窗 pitch 未断 | 词条正确；`DrawGlyph_Chinese_Adv`：`last_adv != adv_px` 时 `pitch_reset`，避免 spill 互叠 |
| 头标仍日文（もちもの/リボン/おぼえているわざ…） | 未挖 / 模块错挂 | `もちもの` 等在缓存里已有译文，但部分挂在「标题与主菜单」；见 `MODULE_BANDS` — **勿**塞旧 `summary_ui` |

地址带：`0x3E9ACB–0x3EA76B`（能力页短标池）。

#### 能力页短标对照（成品 `*_translated.gba` / lexicon）

| JP | ZH | 通道 | 备注 |
|----|-----|------|------|
| `やめる` | 取消 | F9 phrase | lexicon OK |
| `なし` @ `0x3E9AEC` | 无（原误取消） | F9 phrase | 已纠 lexicon |
| `きりかえ` | 切换 | F9 phrase | OK |
| `げんざいのけいけんち` | 当前经验值 | F9 phrase | OK；叠字靠 pitch 断步进 |
| `レベルアップまで` | 直到升级 | F9 | 缓存有译 |
| `ポケモンのうりょく` | 宝可梦能力 | F9 | OK |
| `もちもの` / `リボン` | 携带物品 / 奖章 | 缓存有译 | 头标是否上屏取决于模块勾选/指针，勿 1+1 塞针 |

---

## 八、BUG 台账

### BUG-01 标点拉伸（已修）

| 项 | 内容 |
|----|------|
| 现象 | 对话「。」「？」被纵向拉成细高椭圆 |
| 根因 | Sym 字库误按 16×16 packed 2bpp 解包 |
| 修复 | Font3 式 U/L 各 32B → CHS TL/BL，`DrawGlyph_Chinese_Adv(..., 8)` |
| 落点 | `print_next_char.c` `draw_sym_punct`；见 §7.2.1 |

### BUG-02 战斗昵称 HP 条遮罩（进行中）

| 项 | 内容 |
|----|------|
| 现象 | 对战 HP 条：野名被绿块盖住 / 空白、己方昵称截断+乱码叠字 |
| 根因（Font Patch） | 昵称经 `TextPrintBattleInterface…` 写入 buffer 后，用 `CpuSet` 刷 healthbox 元件（含空白遮罩 `0x2B`）；**整 tile 拷 32B（8 word）盖住 10/12px 字上半** |
| Font Patch 对照 | US `UpdateNickInHealthbox` 共享池 `0x04000008` → `0x04000006`（6 word = 24B） |
| 日版地址 | **不是**美版 `0x080451A0` / 错误 Δ `0x08045138`。JP 三处平行 nick 遮罩：`0x080415A0` / `0x08042408` / `0x08042B14`；池 `0x08041760` / `0x08042620` / `0x08042C38` |
| 修复落点 | [`src/battle/UpdateNickInHealthbox/`](configs/POKEMON_RUBY_AXVJ00/patch/src/battle/UpdateNickInHealthbox/)（目录约定见 [`HOOKS.md`](configs/docs/HOOKS.md)）：`hook_origin.s` 三池→`0x04000006`；C 常量助手进 `game.bin` |
| 非本轮 | Safari / PSS；整函数 C 重写；盲贴美版 `+0x1EA/+0x228` |

#### 验址（origin 只读，2026-07-29）

| 符号 | `game_addrs` | origin 核对 |
|------|--------------|-------------|
| `ProcessCurrentChar_RegularGlyph` | `0x0800336E` | 入口字节存在；`main.asm` → `PrintNextChar` |
| `GetStringWidth` | `0x08004CC0` | `push {lr}` 序言；薄壳挂 `GetStringWidthChinese` |
| `UpdateNickInHealthbox_Alt1` | `0x080415A0` | 序言 `f0b557464e464546`；`mov #0x2B`；池 `0x08041760`=`0x04000008`；`ldr r2` 引用 OK |
| `UpdateNickInHealthbox_Alt2` | `0x08042408` | 同上；池 `0x08042620` |
| `UpdateNickInHealthbox` | `0x08042B14` | 同上；池 `0x08042C38` |
| `hook_origin.s` | — | 已 `.include` 进 `main.asm` |

**结论：地址正确**，可继续绘制侧诊断。

#### 数字旁路 A/B（TEMPORARY，2026-07-29）— **无效 / 未进盘**

曾对 `0xA1..0xAA`、`0xBA` 做 `return 0`。事后核对：本地 `patch/out/game.bin` 含旁路，但用户所测 `*_translated.gba` 在 `0x08800000` **与本地 bin 不一致**（首差 `@+0x44`），ROM 内无 `0xa1`/`0xba` 立即数 → **A/B 未真正执行**。

HP/Lv 路径（pret）：`UpdateHpTextInHealthbox` → `RenderTextHandleBold(eBattleInterfaceGfxBuffer, …)` → 窗口打印 → **`PrintNextChar`**（与昵称同进统一钩子）。

#### JP-via-CHS 总闸 A/B（2026-07-29）— **通过 → 已收窄**

总闸：`draw_jp_via_chs` 一律 `return 0`（可印 PCS 全回 `FontFuncTable`）；F9 中文不变。`font_patch` 嵌入校验 ROM `@0x08800000` ≡ `out/game.bin`。

| 自测结果 | 含义 |
|----------|------|
| 对战 HP 条恢复正常 | **统一 CHS 分配器破坏 healthbox** → 坐实 |
| 详情页乱码 | 全关误伤：详情需要 JP-via-CHS（F9 中文 + 日文 PCS 同池，见 `CHS_TILE_LAYOUT.md`） |

**现行（收窄，勿再全关）**：`textMode`（`WIN_TEXTMODE=+0x0A`）**== 2** → FontFunc（`RenderTextHandleBold` / FontFunc[2] healthbox）；其余可印 PCS → JP-via-CHS。F9 在 textMode==2 时同样交回 FontFunc。

**dest 区间闸（未兑现）**：曾用 `tileData∈[0x02020004,+0x1000)`；详情好但对战仍坏 → PrintNextChar 时该判定未命中。改 `textMode==2`（同路径 `strb #2,[win,#0x0A]`）。

#### 双链路定案（产品铁律，2026-07-29）

对战框已正常；详情大体恢复。分流**已落地**，不按「是否对战场景」另开闸：

| 条件 | 链路 | 落点 | 覆盖 |
|------|------|------|------|
| `textMode == 2` | **原版日文** `FontFuncTable[2]` | `win+0x20` → `eBattleInterfaceGfxBuffer` → CpuCopy OBJ | healthbox nick / Lv / HP |
| `textMode != 2` | **统一中文** F9(12px) + JP-via-CHS(8px) 同 CHS 池 | `template+0x0C` VRAM | 详情 / 对战菜单 / 对话 |

规律：

1. CHS 只服务窗格 VRAM；往 healthbox IWRAM buffer 画 CHS → 空 buffer + 遮罩残片（星/心）。
2. 详情必须开 JP-via-CHS（与 F9 同池），否则「汉字替换」；旁路只能窄到 `textMode==2`，禁止全关。
3. 对战菜单中文 ≠ healthbox：菜单 `textMode≠2`，走统一中文。
4. 闸门信号：`textMode`（稳定）> `tileData` 区间（曾未兑现）。

**本分流解决不了的剩余问题（中文链路内 / 译表，另案）：**

| 现象 | 归属 |
|------|------|
| 「当前经验值」叠数字 | 同窗 F9 12px + JP 数字 8px；`DrawGlyph_Chinese_Adv` 需在 `adv` 变化时硬断 pitch（文档 §7.3；勿靠再调 textMode） |
| 「取消取消」 | Phrase 撞车（`なし` 误译「取消」与 `やめる` 同码）；lexicon / `texts_translated`（§7.2） |

#### 排查（成品 ROM 只读，2026-07-29）

| 成品 | 三池是否 `0x04000006` | game.bin |
|------|------------------------|----------|
| `*_translated_new.gba`（较早） | **已打上** | 当时未核 |
| `*_translated.gba`（数字旁路后） | **已打上** | **旧 bin**（与本地旁路版不一致） |
| `*_zh.gba` | 仍 `08` | — |
| origin | `08` | — |

结论：池新、bin 旧会造成「以为测了绘制闸其实没有」；现已加嵌入校验。
